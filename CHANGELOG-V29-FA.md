# Changelog فارسی نسخه ۲.۹.۰

این نسخه یک اصلاح هدفمند روی v2.8 است و طراحی، route model، repository v3 و قراردادهای سازمان را تغییر نمی‌دهد.

## داشبورد

- محتوای «برای حل مسئله بعدی آماده‌اید؟» در ابتدای RTL و سمت راست hero قرار گرفت.
- context فضای فعال دیگر شبیه CTA نیست.
- کارت‌های آمار وسط‌چین شدند و CTAهای زیر را با context کامل می‌سازند:
  - `status=draft`
  - `status=submitted`
  - `status=reviewing`
  - `status=revision_requested`

## کنترل‌ها و بازخورد

- حداقل ارتفاع و touch target دکمه‌های Solver برابر ۴۴ پیکسل شد.
- پیکان ادامه wizard در RTL اصلاح شد؛ علامت check مرحله نهایی بدون flip باقی ماند.
- toastهای Solver بالای صفحه نمایش داده می‌شوند و ضربدر بستن در سمت راست RTL است.

## قالب‌های مرجع

- دیالوگ رزومه دعوت تیم به صفحه دعوت‌های واقعی متصل شد؛ role، inviter، expertise، member count و verification از registry مرکزی می‌آیند.
- badge تأیید از boolean نمایشی حذف شد؛ تیم `TEAM-55` مطابق پرونده `VER-TEAM-55` حالت «احراز تیم در حال تکمیل» دارد.
- تاریخچه پیشنهاد از نسخه‌های واقعی همان proposal و workspace ساخته می‌شود؛ هیچ نسخه یا تغییر ساختگی برای شبیه‌کردن تصویر مرجع اضافه نشده است.

## دامنه تغییر

- هیچ تغییر محصولی یا بصری در `/app/org/**`، `/app/organization/**`، `/app/reviewer/**` یا `/app/ops/**` انجام نشده است.
- dependency جدیدی افزوده نشده است.
