# تغییرات نسخه ۲.۷

تاریخ انتشار: ۲۶ مرداد ۱۴۰۵

## دعوت‌نامه‌های تیمی

- اقدام «مشاهده رزومه» به کارت دعوت‌نامه تیمی در داشبورد فردی اضافه شد.
- همان نمای رزومه در صفحه دعوت‌نامه‌ها نیز از کامپوننت مشترک استفاده می‌کند.
- دریافت رزومه، فایل PDF واقعی ایجاد می‌کند و نتیجه اقدام را به کاربر اعلام می‌کند.

## پنجره مشاهده رزومه

- پنجره قبلی با نمای جدید شامل هویت و اعتبار تیم، معرفی، اعضای تأییدشده، حوزه‌های تخصصی، نقش پیشنهادی و فایل رزومه جایگزین شد.
- مدیریت فوکوس، بستن با Escape، بازگشت فوکوس و قفل پیمایش پس‌زمینه با قرارداد Dialog مشترک انجام می‌شود.

## تاریخچه نسخه‌های پیشنهاد

- صفحه قبلی با نمای مقایسه ساختاری نسخه‌ها جایگزین شد.
- Timeline نسخه‌های `v1` تا `v3`، انتخاب نسخه پایه، سه دسته تغییر و جزئیات ثبت خودکار اضافه شد.
- نسخه‌های قبلی فقط‌خواندنی هستند و مسیر بازگشت به پیش‌نمایش و نسخه جاری حفظ شده است.

## تنظیمات

- ریشه نقص اولین نمایش صفحه در هماهنگ نبودن وضعیت آماده Router مستقل و تکمیل Render داخلی برطرف شد.
- خروجی مستقل فقط پس از Commit شدن مسیر دقیق و حذف حالت resolving نمایش داده می‌شود.
- صفحات Solver هنگام جابه‌جایی بین مسیرها با کلید Route مستقل بازسازی می‌شوند تا State مسیر قبلی وارد تنظیمات نشود.
- Layout تنظیمات در آخرین لایه CSS تثبیت شد تا در ورود مستقیم و بازگشت مجدد کامل و هم‌اندازه نمایش داده شود.

## فایل‌های اصلی اصلاح‌شده

- `components/team-resume-dialog.tsx`
- `components/solver-dashboard.tsx`
- `components/solver-profile-experience.tsx`
- `components/solver-workflow-experience.tsx`
- `components/internal/internal-app.tsx`
- `app/page.tsx`
- `app/solver-workspace.css`
- `app/app-shell.css`
- `tests/solver-v27-regressions.test.tsx`
- `scripts/smoke-standalone-interactive.mjs`
