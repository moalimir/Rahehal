# فرض‌ها و مرزهای نسخه

- خروجی حاضر frontend production-oriented با داده و سرویس نمایشی است؛ Backend،
  پایگاه داده، ورود واقعی، OTP، امضای دیجیتال، پرداخت و اعلان متصل نشده‌اند.
- نام شرکت و فرد، شناسه پرونده، مبلغ، SLA و KPI صفحات داخلی ساختگی‌اند.
- نقش‌ها و Permission در UI بازنمایی شده‌اند؛ امنیت واقعی باید در API نیز enforce شود.
- منطق حقوقی مالکیت فکری، مالیات، پرداخت و قرارداد پیش از بهره‌برداری باید توسط
  متخصص حقوق و مالی ایران تأیید شود.
- فونت Embedded نسخهٔ پایه، Estedad، با وزن‌های واقعی ۴۰۰ تا ۸۰۰ در تمام سطوح
  عمومی و داخلی محصول استفاده شده است.
- تب‌های «اثر» و «تاریخچه» در پروندهٔ نمونه تا عبور از مرحلهٔ پایلوت قفل‌اند؛
  routeهای مستقیم برای QA و نمایش کامل محصول همچنان در Static Export موجودند.
- سه سکشن مشخص‌شدهٔ لندینگ بازطراحی شدند؛ هدر، Hero، Footer و دیگر بخش‌ها بدون
  بازطراحی باقی ماندند.
- QA تصویری و Routing روی Build استاتیک با Chromium در اندازه‌های تحویلی اجرا و
  نتیجه در `qa-final/qa-report.json` ثبت شده است.
