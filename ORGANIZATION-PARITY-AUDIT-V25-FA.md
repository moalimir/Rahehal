# ممیزی تطابق فضای سازمان با فضای فرد و تیم — نسخه ۲۵

این ممیزی مسیرهای سمت سازمان را با وضعیت‌ها و اقدام‌هایی که فرد یا تیم مشاهده می‌کند تطبیق می‌دهد. تغییرهای نسخه ۲۵ فقط در شاخه تجربه سازمان و اتصال شرطی پوسته اعمال شده‌اند؛ فایل‌های اختصاصی فرد و تیم بدون تغییر مانده‌اند.

| قابلیت دوطرفه           | سمت فرد یا تیم                     | سمت سازمان                                             | تطابق وضعیت و اقدام                                                               |
| ----------------------- | ---------------------------------- | ------------------------------------------------------ | --------------------------------------------------------------------------------- |
| داشبورد و صف اقدام      | `/app/solver/dashboard`            | `/app/org/dashboard`                                   | موعد، شدت، اقدام بعدی و تکمیل کار قابل پیگیری است.                                |
| مشاهده فرصت / ثبت مسئله | `/app/solver/opportunities`        | `/app/org/challenges` و `/app/org/challenges/new`      | چالش منتشرشده در کشف فرصت و پرونده سازمان یک شناسه و چرخه دارد.                   |
| پروفایل و رزومه         | `/app/solver/profile`              | `/app/org/experts`                                     | سازمان پیش از دعوت پروفایل، رزومه، ظرفیت و شواهد را می‌بیند.                      |
| دعوت همکاری             | `/app/solver/invitations`          | `/app/org/invitations`                                 | وضعیت‌های مشاهده‌شده، پذیرفته‌شده، ردشده، در انتظار و منقضی هم‌معنا هستند.        |
| تدوین و ارسال راه‌حل    | `/app/solver/proposals/*`          | `/app/org/proposals`                                   | شناسه، نسخه قفل‌شده، حل‌کننده، چالش و اقدام بعدی بدون بازنویسی نمایش داده می‌شود. |
| مقایسه و شفاف‌سازی      | پاسخ حل‌کننده به درخواست سازمان    | `/app/org/challenges/CH-1405-021/proposals/compare`    | انتخاب چند پیشنهاد و درخواست شفاف‌سازی به همان پرونده نسخه‌دار متصل است.          |
| داوری و تصمیم           | وضعیت پیشنهاد در سمت حل‌کننده      | مسیرهای `/review` و `/decision` پرونده                 | داوری فنی/مالی، تصمیم و موعد مشترک‌اند.                                           |
| قرارداد و مالکیت فکری   | وضعیت قرارداد حل‌کننده             | مسیر `/contract` و `/app/org/contracts-payments`       | نسخه قرارداد، IP/NDA، پذیرش فنی و تأیید مالی جدا و قابل حسابرسی‌اند.              |
| پایلوت و تحویل          | پایلوت، milestone و تحویل حل‌کننده | `/app/org/pilots` و مسیرهای `/pilot` و `/deliverables` | پیشرفت، ریسک، تحویل و درخواست اصلاح متناظرند.                                     |
| پرداخت و رسید           | وضعیت پرداخت حل‌کننده              | مسیر `/finance` و مرکز قرارداد/پرداخت                  | پذیرش فنی از تأیید مالی و تسویه تفکیک شده است.                                    |
| اثر و گزارش             | نتیجه همکاری در سمت حل‌کننده       | مسیر `/impact` و `/app/org/reports`                    | Baseline، هدف، مقدار واقعی و تصمیم توسعه/توقف قابل ردیابی است.                    |
| اعضا و نقش‌ها           | نقش‌های فضای تیم                   | `/app/org/team` و `/app/org/access`                    | مالک غیرقابل کاهش است؛ نقش اعضای دیگر قابل تغییر و رویداد قابل حسابرسی است.       |
| پروفایل                 | پروفایل فرد یا تیم                 | `/app/org/profile`                                     | هویت عمومی از اطلاعات حقوقی و مالی محرمانه جدا شده است.                           |
| تنظیمات                 | تنظیمات فرد یا تیم                 | `/app/org/settings`                                    | حساب، اعلان، امنیت، حریم خصوصی و ترجیحات در اولین رندر آماده‌اند.                 |
| اعلان‌ها                | اعلان‌های فرد یا تیم               | `/app/org/notifications`                               | رویدادهای پیشنهاد، دعوت، داوری و پایلوت با زمان و شناسه مشترک نمایش داده می‌شوند. |

## قیود حفظ‌شده

- هیچ فایل اختصاصی `solver-*` یا `solver-workspace.css` تغییر نکرده است.
- مسیرهای ثبت، تکمیل، پیش‌نمایش و ارسال مسئله سازمانی موجود حفظ شده‌اند.
- مسیرهای عمیق داوری، قرارداد، پایلوت، تحویل، مالی، اثر، اسناد، گفت‌وگو و تاریخچه حذف یا ساده‌سازی نشده‌اند.
- تغییرهای پوسته مشترک فقط با شرط `role === "org"` فعال می‌شوند و رفتار فرد/تیم را تغییر نمی‌دهند.
