/**
 * Local MVP operational default, not an approved retention or product policy.
 * C4 keeps the organization grant narrow and time-bounded while the owner
 * decides the production collaboration lifetime.
 */
export const C4_PROPOSAL_GRANT_DURATION_MS = 30 * 24 * 60 * 60_000;
