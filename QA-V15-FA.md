# گزارش QA نسخهٔ ۱.۵

تاریخ: ۱۹ مرداد ۱۴۰۵

## نتیجه

- TypeScript: پاس
- ESLint: پاس
- تست‌های Vitest: ۸۸ از ۸۸ پاس
- Build تولیدی Next.js: پاس
- Static Export: ۴۱۰ فایل HTML بدون لینک یا دارایی شکسته
- Route Audit: ۲۴۸ مسیر ثبت‌شده تأیید شد
- Standalone آفلاین: Hydration و تعامل‌های اصلی پاس

## کنترل اصلاحات این نسخه

- سایدبار Solver دیگر بلوک «راهنما و پشتیبانی»، عکس و نام تکراری کاربر را رندر نمی‌کند.
- Workspace فردی نام شخص را تکرار نمی‌کند و فقط «فضای شخصی» نمایش داده می‌شود.
- نوار بالای Solver فقط یک نشان Monogram فشرده برای دسترسی به پروفایل دارد.
- تمام نمونه‌های `PersonAvatar` به Monogram فارسی پایدار و بدون عکس واقعی بازگردانده شدند.
- پروفایل تیم از حروف نام تیم استفاده می‌کند، نه تصویر یا هویت مدیر تیم.
- لوگوهای سازمان در قاب مربع با `object-fit: contain`، فاصلهٔ داخلی ثابت و منبع محلی نمایش داده می‌شوند.
- صفحهٔ تنظیمات دارای فرم و Grid تمام‌عرض با `minmax(0, 1fr)` است و در ۹۸۰ پیکسل به پایین تک‌ستونه می‌شود.
- منوی تنظیمات هویت تکراری کاربر را نمایش نمی‌دهد و همهٔ پنج بخش حساب، اعلان، امنیت، حریم خصوصی و ترجیحات حفظ شده‌اند.

## محدودیت نشست

سطح پیش‌نمایش تصویری مرورگر در این نشست در دسترس نبود. بنابراین کنترل نهایی دیداری با Build تولیدی، تست‌های DOM/CSS، Route/Link Crawl و آزمون Standalone انجام شد؛ اسکرین‌شات تازه تولید نشد.
