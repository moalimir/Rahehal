# Delivery Roadmap

**Active plan — 2026-09-16:** DEC-2026-018 replaces the former Phase-4-based MVP. Build from Phase 3 commit `3f80192` using [26_LEAN_MVP_SCOPE](26_LEAN_MVP_SCOPE.md): M0 clean baseline/runtime, M1 owner-controlled publication, M2 minimal selection/final match, M3 connected organization/solver completion, M4 acceptance. M1–M4 are not implemented by this documentation update. The older phase tables and dated reviews below remain historical evidence or deferred design; they must not expand the active MVP scope.

**Revised 2026-08-27 — pragmatic, MVP-first (numeric phases retained).** Earlier this plan front-loaded full production hardening (managed-OIDC+MFA, RLS, malware scanning, durable outbox/DLQ, observability, DR drills, pen test) into Phase 1, before the product journey worked end-to-end. That is backwards for reaching a usable MVP. This version keeps the numeric Phase 0–6 structure the rest of the docs use, but **re-scopes Phase 1 to a lean foundation and pulls the hardening out into one pre-pilot gate** (§9). The hardening is not dropped — it is re-sequenced and detailed in [70_SECURITY_AND_AUTHZ](70_SECURITY_AND_AUTHZ.md).

This document owns delivery order, milestone dependencies, and acceptance gates. [82_PHASE0_COMPLETION](82_PHASE0_COMPLETION.md) owns Phase-0 evidence; [90_REQUIREMENTS_TRACEABILITY](90_REQUIREMENTS_TRACEABILITY.md) maps requirements to milestones; [25_DECISIONS](25_DECISIONS.md) owns decisions; [27_PHASE1_OWNER_APPROVALS](27_PHASE1_OWNER_APPROVALS.md) owns owner sign-offs.

---

## 1. Target outcome

The MVP proves one organization–solver journey, working locally on real persistence:

> An organization owner creates and publishes a challenge → an eligible solver submits an **immutable** proposal version → the parties clarify or revise if needed → the organization selects a solver and records the agreed final outcome → both parties see consistent status and notifications. Every sensitive action remains authorized **server-side**, versioned, and audited.

That is the active MVP: the restored Phases 1–3 plus M1–M4 in [26](26_LEAN_MVP_SCOPE.md). The accepted final flow is organization selection → explicit solver acceptance → confirmed match, with a shared version-bound summary and no routine platform approval. Rejection, decline, withdrawal, cancellation and no-award are required; only detailed role/timing/cardinality and state mappings remain to be specified. Formal Phase 4 review is deferred. Execution/payment and an external pilot come later, with relevant hardening required before real users/data.

## 2. How we sequence (the pragmatic split)

We deliberately separate **load-bearing architecture** (do now — cheap now, a rewrite to retrofit) from **production hardening** (do at the pre-pilot gate). This is an owner-accepted risk decision for a **local build with synthetic data and no real users**; the hardening gate (§9) re-enters before any real org or confidential data is exposed on a server.

| Keep now — Phases 1–5 (mostly already built in `apps/api`)         | Defer to the pre-pilot hardening gate (§9)                                     |
| ------------------------------------------------------------------ | ------------------------------------------------------------------------------ |
| Server-side deny-by-default authorization                          | MFA, step-up freshness, session rotation families, KYB/verification workflow   |
| Tenant/workspace scoping + `access_grant` (data-model correctness) | PostgreSQL RLS as defense-in-depth (app-scoping is enough locally)             |
| Real PostgreSQL persistence                                        | File malware scanning + quarantine (start: private storage + type/size checks) |
| Immutable submitted versions + a basic audit table                 | WORM audit export; durable outbox with poison-isolation/DLQ sophistication     |
| Idempotency keys + optimistic `expected_version` (already present) | Observability stack (SLOs, traces, error budgets), WAF, CSP, rate limiting     |
| One **simple** real login (no MFA/KYB/step-up)                     | Backup/restore/DR drills, measured RTO/RPO                                     |
| Non-enumerating `NOT_FOUND` for protected records                  | External pen test, PIA, WCAG 2.2 AA audit, qualified legal/privacy review      |

**Why the left column stays even in an MVP:** these are the _shape of the app_, not security polish. Skipping server authority or tenant scoping now means building a throwaway and re-touching every table, query, and screen later. Everything on the right is genuinely additive and can be layered on without redesign.

### Status vocabulary

`not-started` · `ready` · `in-progress` · `verification` (built; evidence/review pending) · `done` (built + tested + owner-accepted where required). The old `P1-F# / P2-C# / …` milestone IDs are superseded by the phase sections below; [90_REQUIREMENTS_TRACEABILITY](90_REQUIREMENTS_TRACEABILITY.md)'s milestone column still cites them and is remapped in a follow-up (not delivery-blocking).

### Milestone-letter convention

Each phase's milestones share one letter, sequenced within it: **A** = Phase 1 (done above), **B** = Phase 2, **C** = Phase 3, **D** = Phase 4, **E** = Phase 5, **G** = the pre-pilot hardening gate (§9), **F** = Phase 6. Letters are stable identifiers for cross-referencing from `25_DECISIONS`, `27_PHASE1_OWNER_APPROVALS`, `90_REQUIREMENTS_TRACEABILITY`, and PR/commit messages — they do not imply a fixed calendar order across phases beyond the dependency chain in §1.

## 3. Phase 0 — baseline (essentially closed)

Engineering decisions, the repo baseline, canonical convergence, the in-memory API/worker boundary proof, the local Docker stack, and DEC-2026-010/011 owner sign-off are done ([82_PHASE0_COMPLETION](82_PHASE0_COMPLETION.md), [27_PHASE1_OWNER_APPROVALS](27_PHASE1_OWNER_APPROVALS.md)). The remaining Phase-0 items are **not MVP blockers** and run in parallel: visual Golden Master (`test:visual`), first clean Linux CI run, and placeholder/brand/asset/legal sign-offs before any external distribution.

## 4. Phase 1 — lean foundation (re-scoped)

**Goal:** make the boundary real — one login, one database, the web talking to the API — so the MVP journey has something authoritative to run on. Reuse the ports and in-memory logic already in `apps/api`/`apps/worker`; swap the storage, not the design. **Hardening (MFA, RLS, scanning, WORM, observability, DR) is deferred to §9** — not built here.

**Owner:** Backend + Platform · **Review:** Security, Frontend

| Milestone | Deliverable                                                                                                                                                                                                                              | Status | Acceptance                                                                                                                                                                                                                                                                                                                |
| --------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **A1a**   | PostgreSQL foundation: exact Compose pin; checksummed reversible SQL migrations; tenant/user/identity-link/workspace/membership/`access_grant`/session/challenge+version/audit/outbox/idempotency tables; deterministic synthetic seeds. | `done` | `npm run test:postgres` proves migration down/up through `0012`, seed rerun, and negative constraints (67 tests). Pinned-image startup verified 2026-08-31: rebuilt images and the full Compose stack reach `healthy`; `database-setup` reports `0012_phase2_review_closure`.                                             |
| **A1b**   | PostgreSQL session/workspace/access-audit adapters; explicit transaction-scoped unit of work; digest-only credentials; transaction-time session/membership revalidation; injected test-only OIDC exchange pending A2.                    | `done` | PostgreSQL integration tests prove allow/deny, expiry/revocation, concurrent replay, locks, atomic evidence, and rollback. Security review completed 2026-08-29 (see §17).                                                                                                                                                |
| **A1c**   | Scoped PostgreSQL challenge create/read/save; immutable versions; atomic durable receipt/audit/outbox/idempotency result; explicit PostgreSQL runtime with no demo fallback.                                                             | `done` | Native integration proves concurrency, scope denial, stale conflict, rollback, and API-runtime restart. Pinned container smoke verified 2026-08-29: the containerized API served the challenge slice against the containerized PostgreSQL, with migrations and seeds applied by `database-setup`.                         |
| **A2**    | One **simple** real login against a standards-compatible local OIDC test provider: authorization code + PKCE, server-validated revocable session, verified test contact. MFA/KYB/step-up remain deferred to hardening.                   | `done` | Native/fake-provider tests prove reuse `403` and expiry denial; live Docker/Dex browser acceptance proves sign-in and revoke against the containerized stack. Clean registry-backed rebuild completed 2026-08-29. Security review completed 2026-08-29 (see §17).                                                         |
| **A3**    | Wire the web to the real API: network gateway composition, `/me` + active-workspace switch, and typed error/conflict states. Keep static export as demo-only.                                                                            | `done` | `RAHHAL_CONNECTED_E2E=1 npm run test:browser:connected` passes 3/3 against the containerized stack (sign-in → workspace activation → create → reload-from-PostgreSQL, revoked-session denial, non-enumerating `NOT_FOUND`). Registry-backed rebuild completed 2026-08-29. Security review completed 2026-08-29 (see §17). |

**Phase gate:** a person signs in, creates a challenge, it persists in Postgres, and reloading shows it — with the server (not the browser) enforcing who may do what. Idempotent commands + `expected_version` conflicts already hold (kept from the in-memory design). **The gate is met**: the created row carries a matching audit event, mutation receipt, and outbox event on one correlation ID; clean Docker images pass restart persistence and the connected 3/3 browser suite. No Phase-1 implementation item remains before Phase 3.

### Phase-1 verification notes (closed 2026-08-30)

- **Container rebuild is closed.** The apparent registry stall was Docker Desktop's credential helper, not WireGuard or BuildKit source caching. Clean native `linux/arm64` API/web/worker images were rebuilt from the pinned Node base, and every Compose dependency reached its expected healthy/completed state.
- **`dex.localhost` is only mapped inside the API container.** `compose.yaml` gives the `api` service `extra_hosts: dex.localhost:host-gateway`, but the browser leg of the authorization redirect runs on the host. Chromium resolves `*.localhost` to loopback per RFC 6761 so the flow works there; other browsers and any host-side tooling need an explicit `/etc/hosts` entry.
- **PostgreSQL host-port selection is unified.** Compose publishes `${RAHHAL_POSTGRES_PORT:-5433}` and the native PostgreSQL test support derives its endpoint from the same variable; `test:postgres` loads the repository `.env`, so the current `5543` override and the container mapping stay aligned.
- **Production guards are keyed on `NODE_ENV`, and every image ships `development`.** `browserSessionRuntimeSettings` only requires HTTPS when `NODE_ENV === "production"`, and `oidcRuntimeSettings` only rejects HTTP issuers there. That is correct for the local HTTP stack, but nothing enforces flipping it. Tracked as a hardening-gate item in §9.

## 5. Phase 2 — authoritative challenge (build lean)

**Goal:** carry the A1c challenge draft through the rest of the canonical lifecycle to `published`, with a structurally separate public projection. Reuse the A1c unit-of-work/idempotency/audit pattern verbatim — Phase 2 is new tables and transitions, not a new architecture.

**Owner:** Backend + Product · **Review:** Security, Frontend, Operations

**Status:** complete for the MVP-first Phase-2 boundary (2026-08-30). That boundary is challenge authoring through governed publication, public projection, the live-call controls implemented below, and the platform approval work queue. Post-publish material amendment and publication override are explicitly later exception paths; their unresolved policy does not make the completed routine publication slice incomplete.

| Milestone | Deliverable                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Status | Acceptance                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| --------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **B1**    | Extend the challenge state machine through `triage` → `formulation` → `approvals`. A five-field `triage_readiness` contract gates the rough brief; the full shared readiness contract gates formulation completion (closes FR-ORG-003).                                                                                                                                                                                                                                                                                                                                                                                                                                                        | `done` | Invalid transitions are rejected by the state machine. API tests prove the rough brief may enter triage while a full-readiness issue still blocks `:request-approvals`; draft/preview/full-submit equivalence remains tested. `platform:ops` can see only its purpose-scoped triage item, advance it through the visible queue/governance flow, and immediately loses that brief after the record returns to organization-owned formulation.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **B2**    | `challenge_approval` table: one row per `(challenge_version_id, gate)` for `technical`/`finance`/`legal`/`quality`, each recorded by a distinct actor (separation of duty, [70_SECURITY_AND_AUTHZ](70_SECURITY_AND_AUTHZ.md) §6). Includes a narrow cross-tenant path so `platform:ops`/`finance`/`legal` can record their gates on an org's challenge without org membership.                                                                                                                                                                                                                                                                                                                 | `done` | In-memory and native PostgreSQL integration tests prove: the same actor cannot record two required gates on one version; a duplicate gate is rejected; `publication_readiness.missing` names the outstanding gate(s); concurrent duplicate submissions serialize to one winner; append-only trigger and rollback hold. The committed B2 range completed a no-findings security diff review.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **B3**    | `eligibility_rule` table: versioned `allowed_applicant_types`, verification/NDA/document requirements, deadline, tied to the exact challenge version it governs.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `done` | Migration `0007` introduced/backfilled append-only rules. Review closure migration `0012` removes the per-autosave trigger; the explicit `:request-approvals` transaction now snapshots one fully ready governed version. PostgreSQL tests prove drafts do not mint policy rows, the snapshot is immutable, and publication requires it. The snapshot remains static approval evidence; Phase 3 C1/C4 must also consult the aggregate's current live state/deadline after pause, close, expiry, or extension.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **B4**    | Atomic publish transaction: lock the approved version immutable (`published_version_id` set, all B2 gates present), write `challenge_public_projection` from an explicit allowlist, emit outbox `challenge.published` — one transaction, same shape as A1c's `recordMutation`. Publication is an explicit `publish()` port command (not a fourth `ChallengeTransitionCommand`) restricted to `org:publisher`; migration `0008` adds the projection table with one column per allowlisted field, an append-only trigger, and a `challenge` trigger that refuses any `published_version_id` the four gates never cleared. Only `public`/`registered` challenges receive a projection row at all. | `done` | In-memory and native PostgreSQL tests prove: an under-approved or non-publisher publish is refused with no rows written; a concurrent publish race yields one published version, one projection row, and one receipt; a forced mid-commit failure leaves zero partial rows across challenge/version/projection/audit/outbox/idempotency; the projection's exact column set is asserted and carries no confidential field; a direct SQL publication without four approved gates is rejected by the database. Security review completed 2026-08-29 (§17): no correctness or exposure defect. The review initially held this row on `70_SECURITY_AND_AUTHZ` §6 appearing to require a structured reason and to grant publish to `platform:ops`; re-reading [95 §2](95_RISKS_AND_OPEN_QUESTIONS.md) showed that row described the unimplemented **override** path, not routine publication. §6 is now split into `challenge:publish` (org:publisher, no separate reason) and `challenge:publish-override`, and the implementation matches the former exactly. |
| **B5**    | Public discovery API serves only from `challenge_public_projection`; the private aggregate is never queried on an unauthenticated path. `GET /api/v1/public/challenges` (immutable published-time keyset cursor, normalized optional `category`) and detail read a separate `PublicChallengePort`. Audience is decided server-side from session presence. Free-text `q` search remains Phase 3.                                                                                                                                                                                                                                                                                                | `done` | Native tests prove the public adapter still works with private aggregate tables renamed, exact response fields contain no confidential value, audience visibility is non-enumerating, category matching is trimmed/case-insensitive, and pagination remains stable when deadlines change. Server time excludes expired records from discovery while detail reports derived expiry. Migration `0012` also requires projection inserts to match the aggregate, closing the prior insert-only SQL bypass. Public-endpoint rate limiting remains a gate-G item, not a B5 gap.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **B6**    | MVP live-call controls: extend deadline, pause, resume, close, and cancel. Every command carries an explicit reason and version; server time decides deadline races.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | `done` | Migration `0009` moves live state/deadline onto `challenge`, leaving the approved version and eligibility snapshot untouched. Commands are `org:publisher`-only, idempotent, version-aware, and atomic with receipt/audit/outbox. Migrations `0010`/`0012` require projection updates/inserts to match the aggregate and direct publication to have a future deadline. Tests prove forward-only deadlines, derived expiry, terminal close/cancel, pause/resume discovery, role denial, rollback, projection synchronization, and supported worker events. Material amendment and `challenge:publish-override` remain separate future exception paths.                                                                                                                                                                                                                                                                                                                                                                                                     |
| **B7**    | Real-browser E2E: org drafts → ops screens triage → approvals → publish; the publisher manages the live call; public discovers and visits it; every required actor uses a visible server-backed workflow.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `done` | `RAHHAL_CONNECTED_E2E=1 npm run test:browser:b7` passes 3/3 against rebuilt Docker images and covers the ops triage action plus four distinct gate actors through visible forms. The publisher publishes, pauses, resumes, extends the deadline, and sees confirmed terminal controls; an anonymous context reads only the projection. Negative controls cover under-approved publish, distinct-actor enforcement, and non-enumerating unknown/unpublished reads.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |

### Two tracks, one phase

A Phase-2 milestone is not delivered when its API is authoritative — it is delivered when a person can walk it. The two are tracked separately here, because counting only the first hides a widening gap:

| Milestone            | Server authority            | Connected browser                                                   |
| -------------------- | --------------------------- | ------------------------------------------------------------------- |
| B1 lifecycle         | built                       | org submit + platform-ops screening/advance wired                   |
| B2 approval gates    | built                       | org governance page + platform role-scoped queue and approval brief |
| B3 eligibility rule  | built (approval transition) | n/a                                                                 |
| B4 publish           | built                       | publish action on the governance page                               |
| B5 public discovery  | built                       | server-backed catalogue + public detail projection                  |
| B6 live-call control | built                       | reasoned deadline/pause/resume/close/cancel publisher controls      |

B7 closes the governed publication journey end to end. In the connected runtime, `/challenges` now lists B5 public projections with category filtering and cursor pagination; it never maps them through the richer fixture model. The static demo keeps its existing fixture catalogue. Free-text search, solver-specific fit, saving, comparison, and eligibility remain Phase 3 because their server authorities do not exist yet.

### B8 closure — platform approval brief and work queue

B2's standing platform authority now has the two read surfaces needed to make triage and gate work usable without widening the organization aggregate:

- **B8a:** `GET /api/v1/platform/challenges/{challengeId}/approval-brief` returns a closed read model only for the active actor's current triage purpose or approval gate. Each purpose has its own content allowlist. The brief omits contact details, invitees, attachment ids, tenant/creator ids, and other approvers' user ids; separation of duty is represented by a boolean for the current actor. The org-only `GET /api/v1/challenges/{id}` has no platform fallback.
- **B8b:** `GET /api/v1/platform/challenge-approvals` lists at most 50 ops-triage items or challenges awaiting the active platform role's own gate. It excludes versions on which that actor already recorded a gate. The connected `/app/ops/publication` screen activates the platform workspace, loads the queue, and links to the scoped brief; ops advances triage there through the same server command and loses the brief after success.

API, PostgreSQL, contract, and browser tests cover both surfaces. Migration `0012` contains the Phase-2 review-closure database backstops. Final rebuilt-Docker, restart-persistence, A3, and expanded B7 results are recorded in [82](82_PHASE0_COMPLETION.md) after the acceptance run.

**Phase gate:** an incomplete, unauthorized, or under-approved challenge cannot be published via a direct API call. Published output is structurally separate from the private aggregate and exactly matches the approved version.

## 6. Phase 3 — authoritative proposal (build lean)

**Goal:** let a contact-verified human activate their solver identity, work through a permanent individual workspace or an owned team workspace, and complete one connected organization↔solver proposal journey against a published challenge's exact terms. Submission uses the same create/immutable-version/atomic-evidence shape as A1c, applied to `proposal`/`proposal_version`; the network frontend consumes that authority without browser persistence or demo fallback.

**Owner:** Backend + Frontend + Product · **Review:** Security

**Foundation prepared:** migrations `0013`–`0018` establish solver-workspace proposal ownership, independent aggregate/content versions, assigned-membership references, full content shape with integer minor-unit money, immutable exact challenge-version acceptance, C1 facts, C2 team authority, C4 submission/grants, C5 controlled clarification/revision evidence, and C6 saved-opportunity/direct-offer authority. C3–C6 exercise the foundation through typed demo/PostgreSQL adapters and routes. The canonical proposal/direct-offer transition and team-permission tables each have one shared implementation. C1–C6 remain in `verification`; owner sign-off, independent assurance, notifications, connected UI, and browser certification remain later gates.

| Milestone | Deliverable                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Acceptance                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **C1**    | Authoritative individual/team profile and verification facts plus the server-side `evaluateEligibility` port. Durable workspace facts cover canonical applicant type, profile readiness, expertise, geography, the status-driven verification record, and accepted NDA/document gates; the API evaluates those server-owned facts against the exact B3 `eligibility_rule` and the challenge's live state/deadline, returning explainable reasons/next-actions (FR-IAM-003, FR-SOL-001/002, DEC-2026-016).                                                                                                                                                       | `verification`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | migration `0014`, typed contracts/OpenAPI, demo/PostgreSQL compositions and scoped Fastify routes are built; native domain/contracts/API tests prove personal/team eligibility, conditional verification, live paused/expired behavior, exact-version synthetic acknowledgements, replay/stale-version, wrong-role and non-enumerating membership/session/ID-swap denial. Focused PostgreSQL migration/adapter tests cover derivation, atomic evidence, append-only acknowledgements and negative scope; they were subsequently executed against the local Docker stack (7 files / 83 tests) after two review fixes — the `0009`–`0014` down/up ladder assertion and the `BEFORE UPDATE` scope of `solver_profile_applicant_type_derived`, whose alphabetical precedence shadowed the immutability trigger. `playwright/c1.pw.ts` adds the connected-boundary acceptance (Dex session, cookie transport, route guards, PostgreSQL) that an adapter-level test cannot reach, and C1 state survives an API/PostgreSQL container restart. `evaluate` and `acceptEligibilityGate` reach a call through `challenge_public_projection`, so a published `nda`/`invite_only` call is refused: B4 gives it no projection row, and until C6's direct offer exists no solver holds a grant to one, which makes refusal the correct deny under [70 §3](70_SECURITY_AND_AUTHZ.md). A regression test pins it — the confidential call is proven live, governed and NDA-gated, then denied indistinguishably from an unknown ID with no acceptance or idempotency row written — so C4/C6 cannot widen it by accident. Same-session security and owner Docker review passed; independent assurance remains before `done`; no G3 file evidence or self-verification is claimed. |
| **C2**    | Team create/invite/request/role-policy/ownership-transfer/removal/archive, porting the existing `decideTeamPermission` matrix server-side ([70_SECURITY_AND_AUTHZ](70_SECURITY_AND_AUTHZ.md) §4).                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `verification`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | migration `0015`, typed contracts/OpenAPI, demo/PostgreSQL compositions, and scoped Fastify routes implement team creation, policy, invitations, requests, role/state changes, transfer, leave, and archive. Exhaustive domain tests cover every owner/admin/proposal-manager/contributor/viewer action plus every policy/assignment switch; API tests prove separate team bootstrap with `not_started` verification, authenticated recipient/requester binding, wrong-role denial, immediate removal/archive denial, owner-transfer sequencing, and policy-controlled proposal-manager profile edits. PostgreSQL tests cover atomic evidence/replay, exact recipient binding, request decisions/reactivation, owner consistency, terminal/delete protection, and a row-lock race between ownership/removal operations. Manager-count-changing commands lock `team_workspace` before membership checks, and deferred constraints require exactly one active owner matching `workspace.owner_user_id`. Native same-session security review and non-Docker tests pass; the owner subsequently exercised the PostgreSQL/Docker boundary and returned the membership-delete and transaction-denial audit refinements now on the branch. Independent assurance remains before `done`. C8 owns notification projection and C9 owns connected UI; no managed step-up, file evidence, or shared team credential is claimed.                                                                                                                                                                                                                                                                                                                                            |
| **C3**    | Workspace-scoped `proposal` draft create/read/save behind A1c's unit-of-work pattern; attachments are recorded as metadata references only — private object storage with quarantine/scan is G3's scope, not Phase 3's.                                                                                                                                                                                                                                                                                                                                                                                                                                          | `verification`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `POST /proposals`, `GET /proposals/{id}`, and `PATCH /proposals/{id}` are typed and composed for demo/PostgreSQL runtime. Creation requires a registered-reachable public projection plus the published aggregate's live open/unexpired state, assigns the current team membership, and deliberately leaves final eligibility to C4. Reads and saves first scope by `(tenant_id, workspace_id)`, then apply current `decideTeamPermission`/assignment; foreign IDs, viewers, and unassigned contributors fail non-enumerating. Every save appends an unlocked exact-base version with computed diff/hash and atomically advances the aggregate with receipt/audit/outbox/idempotency evidence; concurrent creates/saves are covered by the authored PostgreSQL suite. Native typecheck and domain/contract/API/worker tests pass, and same-session security review found no unresolved code finding after restoring C2 denial-audit behavior and explicitly scoping version-history reads. PostgreSQL/Docker execution has since run: the suite was unrunnable as authored, and executing it exposed a save-race defect where the losing save received `404 NOT_FOUND` instead of a `409 CONFLICT` carrying `refetch_and_retry`, fixed in `c44c7d9`. C3 PostgreSQL is now 5/5 (full suite 97/97) with a 29/29 live route check. Owner/independent review remains before `done`; C4 submission, C5 revision, G3 file authority, and C9 connected UI are not claimed.                                                                                                                                                                                                                                                                                            |
| **C4**    | Submission transaction: re-evaluates C1 eligibility against the exact challenge rule and current server state/time, checks sender authority under C2, and requires the declarations selected by that rule; creates the first **locked** `proposal_version`, grants the challenge-owning organization narrow read access to that proposal, and exposes scoped organization inbox/detail reads; atomic receipt/audit/outbox/idempotency/grant.                                                                                                                                                                                                                    | `verification`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `POST /proposals/{id}:submit` and organization inbox/detail reads are typed and composed for demo/PostgreSQL runtime. Submission row-locks and rechecks current C2 authority/assignment before replay, locks exact C1/rule/live-call facts, and uses one database transaction timestamp. Otherwise-eligible unverified workspaces pass only when `verification_required=false`; unmet gates return typed actionable details. Success atomically appends an exact-base locked version, advances the aggregate with tracking/time, creates one version-bound organization read grant, and records receipt/audit/allowlisted metadata-only outbox/idempotency. Database triggers enforce immutable version evidence and grant binding. Native typecheck and focused domain/contract/API/worker tests pass; PostgreSQL coverage includes exact gates, replay/mismatch, stale/double/racing submit, close races, rollback, immutable evidence, and active/expired/revoked/out-of-scope organization reads, while domain tests pin the server-deadline decision. Docker execution has since run and required repairs (see the 2026-09-04 C4 Docker verification entry): migration `0016` could not apply at all, so no C4 behavior had ever reached a database. PostgreSQL is now 103/103 across 9 files with a 22/22 live route check. Owner and independent review remain. The 30-day grant expiry is an operational default pending an explicit duration decision; C5/C8/C9, G3 files, and production RLS remain separate gates.                                                                                                                                                                                                                                  |
| **C5**    | Bilateral clarification/revision flow: an authorized organization actor requests clarification or revision through C4's proposal grant; the owning solver workspace responds through `clarification_requested` → `clarification_submitted` → `revision_draft` → `resubmitted`, with each new version carrying an explicit `base_version_id` and computed `changed_fields` diff.                                                                                                                                                                                                                                                                                 | `verification`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Migration `0017`, typed contracts/OpenAPI, demo/PostgreSQL adapters, and eight commands implement the canonical bridge from submitted through eligibility review, clarification, reviewing, revision draft, and resubmission. Clarification and revision-request records are one-way append-only evidence tied to the exact locked version. Revision start appends an unlocked exact-base copy; edits remain versioned; resubmission enforces the request deadline, locks a new version, computes its diff against the requested locked base, revokes the superseded grant, and creates one replacement grant for the exact resubmitted version atomically with receipt/audit/outbox/idempotency. Native C5 API tests cover replay, stale state, cross-organization denial, exact base/diff, and grant replacement. The full PostgreSQL suite passes 105/105 across 9 files, including C5 evidence immutability and migration up/down coverage. Docker execution and a live route review have since run (see the 2026-09-04 C5 review entry): the eight-command lifecycle, clarification evidence, exact-base revision diffing, and grant rotation all behave as specified, and two defects were fixed — a rejected proposal could not be recorded at all, and the documented local database reset became impossible once C5 evidence existed. One divergence is recorded rather than changed: the organization routes authorize on workspace kind alone, so they neither restrict to nor include the canonical per-transition role lists. Owner/independent review and C8/C9 connected notification/UI remain.                                                                                                                                                |
| **C6**    | Saved opportunities + authoritative `direct_offer`/`offer_response` two-party aggregate with expiry.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | `verification`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Migration `0018`, canonical domain/types, typed contracts/OpenAPI, demo/PostgreSQL adapters, worker allowlist, and scoped Fastify routes implement save/unsave plus offer send/list/read/view/response draft/save/submit/decline/cancel/negotiation and server-time expiry. Send binds the sender-owned exact published version to one solver workspace and atomically creates collaborate-offer/read-challenge grants. Recipient commands consume the C2 team oracle; the sender cannot read an unsubmitted response. Decline/cancel/expiry close both grants, and expiry covers an in-progress draft. Domain/contracts/API/PostgreSQL tests cover readiness, replay/stale versions, exact scopes/grants, team viewer denial, atomic evidence, terminal access, and deadline expiry. Selection remains with the later decision/case transaction. Docker execution and a live route review have since run (see the 2026-09-04 C6 review entry): the full save/offer/response/negotiation flow behaves as specified, and three defects were fixed — an offer to a workspace already holding a standing challenge grant failed outright, deleting an unrelated `access_grant` was silently cancelled, and the eligibility evaluator judged offer grants against a different clock than the one that writes them. Owner/independent review, C8 notifications, C9 connected UI, G3 files, and production RLS remain.                                                                                                                                                                                                                                                                                                                                               |
| **C7**    | Self-service human solver activation and onboarding. A provider-neutral development OTP adapter verifies mobile/email and activates one app identity plus exactly one permanent individual workspace; “individual/team” is only start intent. Optional team intent then uses a separate C2 command to create a team workspace, canonical profile, `team:owner` membership, and `not_started` verification—never another account or shared credential.                                                                                                                                                                                                           | `verification`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Migration `0019`, activation/contact-verification domain and contracts, five typed API operations, development provider, demo/PostgreSQL adapters, and worker events are implemented. Email/mobile start, resend, verify, expiry, lockout, bounded starts/sends, `Retry-After`, exact replay, consumed-assertion denial, returning sign-in, and production refusal are covered natively. Activation atomically creates one provider-linked user, solver tenant, permanent individual workspace/membership, blank C1 profile, `not_started` verification, active session, receipt, audit, metadata-only outbox, assertion consumption, and credential-scoped idempotency record. Same-contact database activation is serialized and uniqueness-backed. Team intent returns a separate `create_team` next action; C2 failure leaves the individual activation usable and retryable, while successful creation/context switch opens the team workspace. Native checks pass and the full PostgreSQL suite passes 116/116 across 11 files, including atomic activation, mobile-only restart sign-in, same-contact concurrency, append-only evidence, and the `0001`–`0019` migration ladder. A live route review has since run (see the 2026-09-05 C7 review entry) covering start, verify, activation, returning sign-in, lockout, and rate limiting; it fixed one Persian-first defect where the contract rejected the Persian and Arabic-Indic digits the provider already normalizes. Owner/independent review remains. C9 owns the connected form/session UI and C10 owns real-browser certification; the managed production provider remains an external release gate.                                                                                        |
| **C8**    | Minimal authoritative Phase-3 workspace summaries and in-app notification read model. Idempotent consumers project proposal submission, clarification/response/resubmission, team invitation/request decisions, and C6 direct-offer events into allowlisted user/workspace-scoped notifications; bounded reads, unread counts, mark-read/all-read commands, and authorized deep links feed solver and organization dashboards. External email/SMS/push delivery stays later work.                                                                                                                                                                               | `verification`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Migration `0020` adds the `notification` read model and `outbox_delivery` evidence; the worker gains a PostgreSQL `OutboxSource` with `FOR UPDATE SKIP LOCKED` claims, leases, retry and dead-letter handling, and a `DeliveryLedger` that commits the delivery record and the projection in one transaction, so at-least-once delivery yields exactly one notification per recipient. A canonical routing table maps allowlisted events to an audience; the projector resolves recipients by reading the aggregate inside that transaction, never from the event payload, and copies no aggregate content. Bounded pages with a stable `(occurred_at, id)` cursor, an authoritative unread count, and mark-read/mark-all-read commands are scoped by recipient before id, so a foreign notification is non-enumerating. Projected facts are immutable and a read notification cannot be unread. Native, API, contract, worker, and PostgreSQL suites pass, and a live review of the whole submit-to-notification path passed 14/14 with the worker running against PostgreSQL. Owner/independent review remains; external email, SMS, and push delivery stay later work, and G4 still owns poison-isolation, WORM export, and correlation search.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **C9**    | Full Phase-3 frontend/backend synchronization and route-truthfulness audit, owned in [84_PHASE3_CONNECTED_MVP_AUDIT](84_PHASE3_CONNECTED_MVP_AUDIT.md). Typed network gateways replace direct browser/demo repositories across session chrome, `/app` workspace resolution, solver auth/onboarding, dashboards, profile/verification/settings, teams, opportunities/saved/offers, proposal list/builder/detail/version/clarification, organization dashboard/inbox/detail, and notifications. Phase-2 challenge/public surfaces receive a regression pass; later-phase pages are hidden or explicitly preview/unavailable. The static export remains demo-only. | `verification` — all five stages have landed and the whole slice has been walked live. Stage 1 built the foundation: a shared typed `GatewayResult`, connected workspace gateways wired into `RuntimeProvider` (null in demo mode so a page shows its preview boundary rather than a gateway that silently returns fixtures), the `/app` workspace resolver, session-aware public chrome, and a route classification locked by a behavior. Stages 2-3 converted the solver dashboard, proposal list and record, teams, saved opportunities and direct offers, the organization inbox and record, and notifications on both sides. Stage 4 added connected sign-in, live application chrome, and solver/organization parity. Stage 5 was an owner browser review that produced identity chips, call-grouped inboxes, a record evidence rail, and sign-out for every connected role. 25 registered routes are now live, up from 12 at the audit baseline. | Met, pending owner acceptance. Every C1-C8 family has been walked end to end against the running stack, including the negative paths: cross-tenant, foreign-solver and unknown-id all answer `NOT_FOUND` without revealing whether the record exists. Fourteen defects were found by running the journey rather than by the tests, most recently a rejected membership request that notified nobody and a `/me` that kept offering a workspace the human had left. Evidence: native 581/581; API 103/103; PostgreSQL 130/130; typecheck, lint, format, workspace boundaries; build, 519 routes, 520 link-checked files, smoke, offline, standalone-interactive; both demo and network budget sets. **Not done:** the six-step proposal builder still writes its draft through the browser repository in demo mode; organization pilots, contracts and reports remain later-phase surfaces; and RTL/accessibility/responsive behavior needs owner visual review, which cannot be self-certified. Three findings are recorded and deliberately open: the organization's challenge list does not distinguish an open call from a closed one, the OTP session exchange answers `select_workspace` where the OIDC path answers `continue`, and the solver dashboard shows two counts under near-identical wording.                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **C10**   | Real-browser connected MVP certification: anonymous landing → self-service OTP signup/sign-in → individual workspace or optional team bootstrap → discovery → eligibility → saved/draft/submit; the challenge-owning organization sees the exact locked version in dashboard/inbox/detail and requests clarification; the solver receives, answers, and notifies the organization.                                                                                                                                                                                                                                                                              | `not-started`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | personal and team journeys pass against API/PostgreSQL across reload and API/container restart; an unverified team submits where `verification_required=false` and is visibly blocked with a next action where `true`; session chrome changes after login and returns to anonymous after sign-out; team bootstrap retry preserves individual activation; ID-swap, wrong-role, revoked membership/grant/session, stale version, duplicate idempotency key, OTP replay/expiry/resend/rate limit, notification deep-link denial, deadline race, API outage, and browser-storage poisoning fail closed; both parties render the same immutable version                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |

**C7 identity/provider constraint:** ADR-0010 remains binding: Rahhal does not store passwords or OTP secrets, and authentication/OTP/recovery stay behind a provider boundary. C7 may reach `done` locally with a development-only provider adapter that proves the activation contract and refuses production startup; external/pilot release remains blocked until the owner-supplied OTP API is reviewed and connected as that provider or an adapter behind it. Using a raw delivery API as app-owned authentication requires an explicit ADR amendment and Security approval. Provider replacement must not change app identity, workspace, membership, or activation command semantics. The existing onboarding-only `formal-company`/`independent`/university variants remain presentation data and must not extend canonical `TeamKind` or enter API/database fields.

**C7–C9 connected UX contract:** preserve the current three-stage signup rhythm but change its meaning to (1) choose start intent, (2) verify the human contact through the provider, and (3) complete the individual profile or optional team bootstrap. The first stage must not say or imply that a team has its own account. The connected form never collects an app-owned password: login has one human OTP flow, and workspace selection happens after authentication. A successful team bootstrap redirects into the team workspace. A failed team bootstrap never asks the user to register again and offers a resumable retry. The active workspace's verification status is visible from its live dashboard and settings summary, with a dedicated verification workflow; `not_started` is a valid state, not an error or a “verified solver” badge. The complete page inventory, including authenticated public header/footer behavior and organization proposal receipt, is binding in [84](84_PHASE3_CONNECTED_MVP_AUDIT.md). Real evidence upload remains blocked on G3 private storage/quarantine/scan; Phase 3 may use synthetic metadata only.

**Phase gate:** a new human can self-activate through the development OTP provider, receive exactly one individual solver workspace, and optionally create a separately owned, initially unverified team workspace without any shared team login. In the connected browser, both contexts can complete the organization↔solver submission, organization-inbox, clarification, and in-app-notification journey against PostgreSQL when the exact challenge policy permits. The authenticated public header/mobile drawer/footer no longer advertise signup/login and expose only a compact role-appropriate panel entry; the `/app` resolver and authenticated application chrome own live identity/workspace presentation and switching. Every connected MVP navigation target is authoritative, all committed state survives reload/API restart, and no failure falls back to fixtures or browser persistence. No cross-workspace read/write is possible by changing IDs; a duplicate submission produces exactly one locked version, grant, receipt, and notification; every revision cites an immutable base version.

## 7. Phase 4 — review, decision, and MVP completion

**DEFERRED in full by DEC-2026-018.** This section retains the former D1–D11 plan for reference; its title, goals, prerequisites, statuses, and phase gate are historical, not active MVP requirements. The archived Phase 4 implementation is not part of the restored baseline. The small selection/match flow will be designed as M2 in [26](26_LEAN_MVP_SCOPE.md), without requiring this programme.

**Goal:** close the loop the MVP promises. A submitted proposal is scored by an independent reviewer against a versioned rubric, behind a COI gate the server enforces; the organization compares those scores without seeing who wrote them; and it records one reasoned decision that cites exact versions and opens a case. At the end of this phase a challenge can travel `published → evaluating → decided` and a proposal can reach `selected` or `rejected` — two canonical states that exist in the domain today with no transition into them.

**Owner:** Backend + Product · **Review:** Security, Operations

> **Two D namespaces exist and they are unrelated.** `D1`-`D11` here are Phase-4 milestones. `D-01`-`D-09` in [00_OVERVIEW](00_OVERVIEW.md) §5 are consolidation decisions, and [20](20_CANONICAL_MODEL.md) §4.3 is titled "Review + COI (D8)" meaning that decision, not this milestone. Cite a decision with its hyphen.

**What Phase 3 hands over.** Locked, append-only `proposal_version` rows verified immutable end to end; version-bound organization read grants; the `decideTeamPermission` matrix server-side; atomic receipt/audit/outbox/idempotency on every mutation; and a connected frontend for both parties. D anchors every assignment, score and decision to an exact `proposal_version_id`, which is why that immutability had to be proven first.

**Three things Phase 3 does not hand over, and D must build before it can build on them:**

1. **No reviewer exists.** `platform:reviewer` is a canonical role with no membership, no seed and no activation path anywhere in the stack. Nobody can sign in as one, so the reviewer leg of the MVP journey cannot be walked at all until D1 creates one.
2. **`ReviewState` lives only in the demo model.** `domain/state-machines.ts` carries the full `coi-gate → accepted → draft → submitted → locked → invalidated` table with its roles, preconditions and side effects, but that is the duplicate legacy model constraint 1 forbids extending. D1 ports it into `@rahhal/domain` exactly as C3/C4 ported `proposalStates`.
3. **The challenge lifecycle stops at `published`.** `evaluating` and `decided` are canonical stages with stated preconditions (`submission-window-closed`; `reviews-complete, decision-rationale`) and no command reaches either. Without them a decision has no stage to live in and `contracting`'s `winner-selected` precondition can never fire.

| Milestone | Deliverable                                                                                                                                                                                                                                                                                                                                                                                    | Status        | Acceptance                                                                                                                                                                                                                                                               |
| --------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **D1**    | **Review foundations.** Port `ReviewState`/`reviewTransitions` into `@rahhal/domain`; migration for `rubric`, `rubric_version`, `review_assignment` (`rva_`), `coi_declaration`, `decision` and `case` as real rows per [20](20_CANONICAL_MODEL.md) §6; a seeded `platform:reviewer` actor with a local session, and the identity/eligibility/workload port behind it. No user-facing surface. | `not-started` | the canonical review vocabulary is imported from `@rahhal/domain`, not from `domain/state-machines.ts`; migration up/down is reversible; a reviewer can authenticate and read an empty assignment list                                                                   |
| **D2**    | **Versioned rubric.** An organization authors a rubric bound to the exact published `challenge_version`; criteria carry weight and valid range; a `rubric_version` becomes immutable the moment any review cites it.                                                                                                                                                                           | `not-started` | a rubric version referenced by an assignment cannot be edited or deleted through any endpoint; a challenge cannot enter `evaluating` without one                                                                                                                         |
| **D3**    | **Evaluation opens.** The `published → evaluating` transition on the challenge, gated on `submission-window-closed` and authored by `org:member`; the organization's evaluation view lists exactly the locked proposal versions that were submitted before the deadline.                                                                                                                       | `not-started` | a challenge still accepting proposals cannot enter `evaluating`; entering it does not mutate a single proposal version; a late submission is absent rather than filtered in the client                                                                                   |
| **D4**    | **Assignment and workload.** `review_assignment` linking one reviewer to one exact `proposal_version_id` and `rubric_version_id`, with a due date; reviewer-scoped listing and workload.                                                                                                                                                                                                       | `not-started` | an assignment is scoped to one exact proposal version; a reviewer's listing never enumerates assignments outside their own set, and a foreign assignment id is unreachable rather than forbidden                                                                         |
| **D5**    | **COI gate.** First-class `coi_declaration` (`pending`/`clear`/`conflict`) per assignment, server-gated at the API, and the retirement of the prototype's `localStorage` flag in `lib/reviews/access.ts` ([20](20_CANONICAL_MODEL.md) §4.3).                                                                                                                                                   | `not-started` | an assignment in `pending` or `conflict` returns nothing from the materials or scoring endpoints — no field, no metadata, no cache leak; only `clear` unlocks `coi-gate → accepted`; `conflict` routes to operations and never yields material access                    |
| **D6**    | **Scoring and locking.** Criterion validation and required rationale across `accepted → draft → submitted → locked`, plus the separated `invalidated` path: ops-only, reason required, distinct actor from both the assigned reviewer and the deciding organization member ([70](70_SECURITY_AND_AUTHZ.md) §6).                                                                                | `not-started` | a score outside a criterion's range, or a criterion missing its rationale, is rejected before `submitted`; a locked review cannot be edited by its reviewer through any endpoint; invalidation is always attributed to an ops actor with a recorded reason, never silent |
| **D7**    | **Blind comparison.** The organization's comparison projection withholds reviewer identity and other reviewers' scores until the policy-defined window opens ([70](70_SECURITY_AND_AUTHZ.md) §6).                                                                                                                                                                                              | `not-started` | the withheld fields are **absent from the API response** before the window opens, not hidden at render time; the same request after the window carries them                                                                                                              |
| **D8**    | **Reasoned decision.** Shortlist, no-award and selection citing the exact `proposal_version_id` and the `review` versions behind it, with authorized actor, structured reason and step-up. Proposals reach `selected`/`rejected`; the challenge reaches `decided`.                                                                                                                             | `not-started` | a decision cannot be recorded twice for one challenge; a stale or duplicate command is rejected with the current state rather than silently accepted; a decision that does not cite a locked review is refused                                                           |
| **D9**    | **Case creation.** An atomic `case` in the same transaction as a `selected` decision, and the full correlation chain `challenge_version → proposal_version → assignment/review → decision → case`.                                                                                                                                                                                             | `not-started` | one authorized request produces exactly one case; the correlation id threads through every row so the chain is reconstructable from any one of them; a failed case insert rolls the decision back                                                                        |
| **D10**   | **Connected reviewer surface and review notifications.** Convert `/app/reviewer` from fixtures to server authority the way C9 converted the solver and organization families, and add the review and decision kinds to the C8 projection allowlist — assignment received, COI required, review due, decision recorded.                                                                         | `not-started` | no reviewer route reads a fixture or browser storage; a reviewer with no assignments sees an empty state rather than a demo one; every new notification kind reaches its recipient in the workspace they read, proven by an event-to-row count                           |
| **D11**   | **MVP browser certification.** The full journey in a real browser across organization, solver and reviewer, plus the negative paths: COI-conflict denial, a stale-version decision attempt, and a duplicate-decision replay.                                                                                                                                                                   | `not-started` | the happy path passes end to end against PostgreSQL; every listed negative path fails safely with the correct typed error, not a 500 or a silent no-op; nothing falls back to fixtures at any step                                                                       |

> **Superseded completion rule:** Phase 4 used to define MVP completion. The active completion gate is now M4 in [26](26_LEAN_MVP_SCOPE.md). Local synthetic-data acceptance still does not authorize real confidential data; that needs §9.

**Phase gate:** a COI-`pending` or `conflict` reviewer obtains nothing at the API; a locked review is immutable except through an audited, separated invalidation; every decision cites exact versions, an authorized actor and a reason; a selected decision opens exactly one case; and `challenge → proposal → assignment/review → decision → case` is traceable end to end from any link in it.

**Owner decisions this phase needs, and when.** Two are already answered in [95](95_RISKS_AND_OPEN_QUESTIONS.md) §4 and need only confirmation; two are open and block specific milestones.

- **Before D5** — the COI relationship set and lookback are answered (employment/affiliation, financial interest, close personal relationship, prior collaboration, advisory role; 24-month default; ops may never turn a `conflict` into access). _Needs legal/product to confirm the lookback._
- **Before D7** — reviewer anonymity is answered (hidden from solvers and other reviewers, visible to ops, pseudonymous to the organization during scoring) **except its timing**, which is recorded as _product to confirm org-visibility timing_. D7's acceptance is defined in terms of when the window opens, so it cannot be built until that is decided.
- **Before D8** — what an organization may learn about the solver behind a proposal, and when. The recommendation is the workspace label and kind during evaluation, legal identity only at selection; implementing it needs a visibility field that `SolverWorkspaceProfileResource` does not have. Today the inbox carries `owner_workspace_kind` and nothing else, which is narrower than [84](84_PHASE3_CONNECTED_MVP_AUDIT.md) §5.4 requires and safe in the meantime.
- **Not blocking, but decide before the MVP is shown:** `withdrawn` is a canonical proposal state with no transition into it, so a solver cannot withdraw a submitted proposal. Whether that belongs in the MVP is a product call rather than an implementation gap.

**Deliberately out of scope**, so the phase gate is not read as more than it is: private object storage with quarantine and scanning (G3) — reviewers score metadata-only attachments exactly as C3/C4 record them; managed MFA and real step-up freshness (G-series) — D8 uses the existing step-up contract against the development provider; row-level security; contracts, pilots, payments and impact, which are Phase 5; and any AI-assisted matching, ranking or summarization, which constraint 9 defers regardless of phase.

## 8. Phase 5 — execution & payment (build lean, Slice 2)

**Goal:** turn a `selected` decision's case into an effective contract, a run pilot, accepted deliverables, and non-custodial payment status — built lean and local like the MVP, with real providers deferred to the hardening gate.

**Owner:** Backend + Finance/Legal (decisions) · **Review:** Security, Operations

| Milestone | Deliverable                                                                                                                                                                                                   | Status        | Acceptance                                                                                                                           |
| --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| **E1**    | Versioned `contract_version`/IP schedule; negotiation → approval → signature → `effective` transitions; provider-backed signature is a stub/local test provider until DEC-2026-007 selects a real one.        | `not-started` | a contract becomes effective only from its approved current version; superseding a version never mutates the superseded one          |
| **E2**    | Participation-scoped case messages/documents: access follows the case's `access_grant`, not a blanket org/solver relationship.                                                                                | `not-started` | a workspace outside the case's grant cannot read case messages/documents even with a guessed ID                                      |
| **E3**    | Pilot plan: milestones, owners, KPI baselines/targets, evidence, change control (`planned` → `running`).                                                                                                      | `not-started` | a plan change after `running` creates a new reasoned version; it never silently rewrites the original baseline                       |
| **E4**    | Deliverable submit/accept/revise/reject protocol; the accepting actor is distinct from the finance approver (separation of duty carried from A1b's audit pattern).                                            | `not-started` | acceptance requires evidence attached to the exact criteria; the same actor cannot both accept a deliverable and approve its payment |
| **E5**    | Non-custodial invoice/payment schedule and status ledger — the platform records status only; the org pays the solver directly (DEC-2026-008).                                                                 | `not-started` | money is stored as `amount_minor` + ISO currency, never a float; no code path moves funds through the platform                       |
| **E6**    | Three-gate payment command: `processing` requires effective contract (E1) + technical acceptance (E4) + a distinct finance approval, checked in one transaction under lock; provider callback reconciliation. | `not-started` | a payment cannot reach `processing` with any one gate missing; a duplicate provider callback produces one ledger effect, not two     |
| **E7**    | Dispute/evidence/SLA/appeal queues with bounded ops access and legal hold.                                                                                                                                    | `not-started` | a disputed case's evidence is retained even if a later, unrelated cleanup job runs; access to dispute evidence is itself audited     |
| **E8**    | Case closure: reconciled payment status, one-time structured feedback per authorized party, impact evidence, case-study consent.                                                                              | `not-started` | closure is blocked while any deliverable or payment remains unresolved; feedback can be submitted exactly once per authorized party  |
| **E9**    | Full execution/payment real-browser E2E plus a provider-outage/replay/reconciliation drill.                                                                                                                   | `not-started` | the E1–E8 happy path passes; a simulated provider outage/duplicate callback is reconciled without a duplicate financial effect       |

**Phase gate:** no payment advances without an effective contract, technical acceptance, and a distinct finance approval. Provider callbacks are reconciled, never trusted as sole authority. See [60_API_CONTRACT](60_API_CONTRACT.md)/[70_SECURITY_AND_AUTHZ](70_SECURITY_AND_AUTHZ.md).

## 9. Pre-pilot hardening gate (before real users/data on a server)

**Goal:** everything Phases 1–5 deliberately deferred, done **before any real organization or confidential data is exposed on a server** and before real-money pilot use. This is a required gate between local building and the Phase 6 pilot — not a redesign; detail lives in [70_SECURITY_AND_AUTHZ](70_SECURITY_AND_AUTHZ.md).

**Owner:** Platform + Security · **Review:** Backend, Frontend, Privacy, Operations

| ID     | Deliverable                                                                                                                                                                                                                                            |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **G1** | Identity hardening: managed in-region OIDC provider, MFA/step-up for sensitive actions, session rotation/revocation families, abuse/rate controls, KYB/verification workflow.                                                                          |
| **G2** | Data defense-in-depth: PostgreSQL RLS behind the app-scoping already in place; multi-party publication approvals (separation of duty); the three-gate payment rule enforced for live money.                                                            |
| **G3** | Files: private object storage with quarantine + malware scan + signed authorized reads (MVP may start with private storage + type/size validation only).                                                                                               |
| **G4** | Durability & audit: durable outbox with poison-isolation/DLQ, append-only WORM audit export, correlation search.                                                                                                                                       |
| **G5** | Ops & edge: structured logs/metrics/traces, Web Vitals, SLOs/error budgets, CSP/security headers, WAF, secret + dependency + container scanning, tenant-isolation CI gates; role code-split + feature-CSS split to lower byte ceilings (DEC-2026-009). |
| **G6** | Environments & recovery: isolated dev/preview/staging/production via IaC, build-once-promote-digest, backup/restore + outbox-recovery + audit-correlation drills with measured RTO/RPO.                                                                |
| **G7** | External assurance: penetration test, privacy impact assessment + data map + subject-request process, WCAG 2.2 AA audit, qualified legal/privacy/finance review.                                                                                       |

**Gate:** no cross-tenant/wrong-role access at API/RLS/object/UI; revocation and membership removal deny immediately; uploads inaccessible until scanned; recovery drills pass; production mode refuses every mock adapter; **every runtime image sets `NODE_ENV=production`** — the HTTPS-origin and HTTPS-issuer checks in `browser-session.ts` and `oidc-authorization.ts` are keyed on it and are inert in the local images (G1/G5); no unresolved critical/high finding.

## 10. Phase 6 — controlled pilot (Slice 3)

**Goal:** admit a deliberately limited real cohort only after the §9 hardening gate is exercised (not assumed). Phase 6 is cohort onboarding and operation, not a second hardening pass — it consumes G1–G7's evidence rather than re-deriving it.

**Owner:** Product + Operations · **Review:** Engineering, Security, Legal/Privacy, Finance

| Milestone | Deliverable                                                                                                                                                                                                          | Status        | Acceptance                                                                                                      |
| --------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- | --------------------------------------------------------------------------------------------------------------- |
| **F1**    | Confirm all G1–G7 evidence is current (not stale from an earlier snapshot) and re-run any drill whose environment changed since it was last exercised.                                                               | `not-started` | every G-item has a dated, linked evidence artifact from within the current release cycle                        |
| **F2**    | Pilot participant selection: a named, bounded set of organizations/solvers/reviewers; data-sensitivity classification per participant matches the residency/classification decisions in `27_PHASE1_OWNER_APPROVALS`. | `not-started` | no participant's data classification exceeds what the approved region/provider set can legally hold             |
| **F3**    | Onboarding runbook: invitation, verification/KYB completion, first-login support path, rollback/feature-flag plan if the cohort needs to be paused.                                                                  | `not-started` | a participant can be paused or removed from the cohort without corrupting in-flight case state                  |
| **F4**    | On-call, incident severity, and privileged-support-access procedures are staffed and rehearsed (not just documented).                                                                                                | `not-started` | a simulated incident is triaged, escalated, and resolved by the named rotation within its defined severity SLA  |
| **F5**    | Instrumented success measures wired to real (not synthetic) pilot data: the funnel metrics in `10_PRODUCT_VISION` §7, SLO/error-budget dashboards, and guardrail metrics together.                                   | `not-started` | a stakeholder can see funnel + guardrail metrics for the live cohort without a manual data pull                 |
| **F6**    | Cross-functional go/no-go: product, engineering, security, legal/privacy, finance, operations each sign off explicitly against the G1–G7 evidence and F1–F5 readiness.                                               | `not-started` | every required signatory has recorded an explicit accept/reject with date, not an implicit absence-of-objection |
| **F7**    | Scheduled pilot review: a fixed checkpoint to assess funnel/guardrail data and decide continue/expand/pause/stop.                                                                                                    | `not-started` | the review happens on schedule with the F5 dashboards as its primary evidence, not ad hoc anecdote              |

**Phase gate:** no unresolved critical/high security, privacy, legal, accessibility, data-loss, operational, or financial finding. Product, Engineering, Security, Legal/Privacy, and Finance sign the controlled-pilot go/no-go.

## 11. Feature readiness — when each capability goes from demo to real

The phase tables above are organized by engineering milestone; this section is the same information organized by **user-facing feature**, for anyone asking "when can I actually use X." **"Real from" ≠ "safe for real data."** Everything through Phase 5 is server-real but built and tested locally with synthetic data — nothing is safe for an actual organization's confidential files or actual money until the **hardening gate (G1–G7, §9)** passes.

### Identity & workspace

| Feature                                       | Real from                 | Notes                                                                                                          |
| --------------------------------------------- | ------------------------- | -------------------------------------------------------------------------------------------------------------- |
| Sign in / sign out, session                   | **A2/C7/C9** (Phases 1/3) | Existing OIDC session plus self-service solver OTP scheme and session-aware frontend chrome                    |
| Active-workspace switching, `/me`             | **A3** (Phase 1)          | Web wired to the real API                                                                                      |
| Team creation/invite/roles/ownership transfer | **C2** (Phase 3)          | Full permission matrix enforced server-side                                                                    |
| Solver registration and first activation      | **C7** (Phase 3)          | Verified human contact → one individual workspace; optional unverified team workspace has no shared credential |
| Individual/team verification status and flow  | **C1/C7** (Phase 3)       | Separate from contact verification; submission gate follows the exact challenge rule                           |
| Workspace dashboards and in-app notifications | **C8/C9** (Phase 3)       | Live counts/actions/read state and authorized deep links; external delivery remains later                      |

### Organization: challenge

| Feature                                   | Real from                 | Notes                                                                                      |
| ----------------------------------------- | ------------------------- | ------------------------------------------------------------------------------------------ |
| Draft a challenge (create/edit/autosave)  | **A1c** (Phase 1)         | Already built — create/read/save only, no lifecycle yet                                    |
| Triage → formulation → approvals workflow | **B1–B2** (Phase 2)       | Distinct-actor approvals: technical/finance/legal/quality                                  |
| Publish (locked, immutable version)       | **B4** (Phase 2)          | Atomic publish transaction; `org:publisher` only                                           |
| Public challenge discovery / search       | **B5** (Phase 2)          | Served only from the public projection, never the private record                           |
| Deadline/extend/pause/resume/close/cancel | **B6** (Phase 2)          | Reasoned live-call controls; projection synchronized                                       |
| Post-publish material amendment           | Future exception          | Requires new-version re-approval and Phase-3 proposal policy                               |
| Send a direct offer to a solver           | **C6** (Phase 3)          | Two-party aggregate                                                                        |
| Submission inbox and clarification        | **C4–C5/C8–C9** (Phase 3) | Grant-scoped reads/actions, dashboard summary, notification, and connected organization UI |

### Solver

| Feature                            | Real from            | Notes                                                                                     |
| ---------------------------------- | -------------------- | ----------------------------------------------------------------------------------------- |
| Eligibility check on a challenge   | **C1** (Phase 3)     | Exact published rule; workspace verification is conditional                               |
| Save an opportunity                | **C6** (Phase 3)     |                                                                                           |
| Draft a proposal                   | **C3** (Phase 3)     |                                                                                           |
| Submit a proposal (locked version) | **C4** (Phase 3)     | Same atomic/immutable pattern as A1c                                                      |
| Clarification / revision           | **C5** (Phase 3)     |                                                                                           |
| Respond to a direct offer          | **C6** (Phase 3)     |                                                                                           |
| Connected individual/team journey  | **C7–C10** (Phase 3) | Activation, notifications, route synchronization, and authoritative browser certification |

### Reviewer

| Feature                      | Real from        | Notes                                                                |
| ---------------------------- | ---------------- | -------------------------------------------------------------------- |
| Assignment + workload        | **D4** (Phase 4) | Scoped to one exact `proposal_version_id` and `rubric_version_id`    |
| COI declaration gate         | **D5** (Phase 4) | Server-gated — replaces the prototype's `localStorage` flag entirely |
| Rubric scoring               | **D6** (Phase 4) | Against the immutable `rubric_version` D2 binds to a challenge       |
| Locked review / invalidation | **D6** (Phase 4) | Invalidation is ops-only and a distinct actor                        |

### Organization: decision

| Feature                               | Real from        | Notes |
| ------------------------------------- | ---------------- | ----- |
| Blind/timed comparison of proposals   | **D7** (Phase 4) |       |
| Reasoned decision (select / no-award) | **D8** (Phase 4) |       |
| Case created on selection             | **D9** (Phase 4) |       |

> **→ This is the MVP.** Draft → publish → discover → eligible → submit → COI → score → decide is real, tested, and demoable at the end of **Phase 4** — with synthetic data, not real customers yet.

### Execution (post-MVP, Slice 2)

| Feature                                 | Real from           | Notes                                                                                                          |
| --------------------------------------- | ------------------- | -------------------------------------------------------------------------------------------------------------- |
| Contract negotiation + signature        | **E1** (Phase 5)    | Real transitions; signature provider is a local stub until DEC-2026-007 picks a real one at the hardening gate |
| Case messages/documents                 | **E2** (Phase 5)    |                                                                                                                |
| Pilot plan / milestones                 | **E3** (Phase 5)    |                                                                                                                |
| Deliverable submit/accept/revise/reject | **E4** (Phase 5)    |                                                                                                                |
| Payment status (non-custodial)          | **E5–E6** (Phase 5) | Status/ledger only — real provider + real money wait for the hardening gate                                    |
| Disputes                                | **E7** (Phase 5)    |                                                                                                                |
| Case closure, feedback, impact          | **E8** (Phase 5)    |                                                                                                                |

### Not features — cross-cutting gates

- **Real file uploads (scanning/quarantine)**, **MFA/step-up**, **rate limiting/WAF**, **backup/DR** are not a product feature — they are the **hardening gate (G1–G7, §9)**, sitting between Phase 5 and Phase 6. Nothing above is safe for a real org's confidential files or real money until this passes.
- **Real users on a server** — **F1–F7** (Phase 6): a named pilot cohort, onboarded only after G1–G7 evidence is confirmed current.

**Active readiness rule:** the organization–solver MVP is complete only when M4 passes. The feature tables above describe the broader historical roadmap, not current certification. Contract→pilot→payment and formal reviewer workflows remain deferred; real users/data require the applicable hardening gate.

## 12. Non-negotiables even in the MVP

These are cheap now and a rewrite to retrofit, so they hold from Phase 1:

1. **The browser is never the authority.** Every mutation is a server command that is authorized, validated, and persisted server-side.
2. **Tenant/workspace scoping + `access_grant`.** Every protected row has one owning tenant; cross-tenant reach only via a grant (DEC-2026-011).
3. **Immutable submissions + an audit row** for every sensitive action.
4. **Idempotency + `expected_version`** on commands (already built).
5. **Canonical vocabulary** stays enforced (`verify:vocabulary` + `verify:boundaries`).

Everything else may be simple, stubbed, or deferred to the hardening gate without guilt.

## 13. Definition of done — two bars

**Accepted completeness additions:** M2/M3 must include the shared agreement summary, explicit solver acceptance/decline, rejection, withdrawal, cancellation and no-award, reliable draft-save feedback/recovery, loading/empty/error states, safe retries and working Persian/mobile navigation. M4 verifies successful and unsuccessful outcomes, stale/changed-term acceptance, competing terminal commands and restart persistence for both parties. Formal platform approval, a payment gateway and real-time chat are not MVP gates. Before external-user validation, a real auth/contact provider and one outbound notification channel are required, along with the existing pre-pilot gates. Determine whether useful proposals require files; if so, deliver the minimal secure private-file journey before accepting real attachments. See [26](26_LEAN_MVP_SCOPE.md) sections 5–6 for accepted scope and remaining detailed policies.

- **MVP done (M4):** owner publication, retained Phase 3 interactions, the agreed minimal final selection/match, and both parties' outcome/notification views work in real connected browsers against PostgreSQL. The acceptance in [26](26_LEAN_MVP_SCOPE.md) and applicable standard gates below pass. Formal reviewer scoring is not required. Acceptance is for synthetic local data; it is not an external-pilot release.
- **Pilot-ready (§9 passed):** the hardening gate passes and the required owners have signed off. Only then does real org/solver data go on a server (Phase 6).

## 14. Standard acceptance gates (unchanged, still mandatory for web/shared changes)

```bash
npm run typecheck
npm run lint
npm run format:check
npm test
npm run verify:boundaries
npm run verify:vocabulary
npm run analyze:source:check
npm run build
npm run verify:routes
npm run verify:links
npm run test:smoke
npm run verify:offline
npm run test:standalone-interactive
npm run check:budgets
npm run build:web:network
npm run check:budgets:network
npm run test:browser
```

Phases 1–4 add: PostgreSQL migration up (and down where safe) tests, API↔database integration tests, generated-contract drift check, and a real browser→API→Postgres journey test. The hardening gate (§9) adds the RLS/durability/security/recovery gates. New scripts are named only when they land.

## 15. What's explicitly deferred (not deleted — hardening gate §9 or later)

MFA/KYB/step-up · RLS · malware scanning · WORM audit / DLQ sophistication · observability stack / WAF / rate limiting · backup/DR drills · pen test / PIA / WCAG audit / legal review · payload reduction (role/CSS split, DEC-2026-009) · AI matching & embeddings ([45](45_AI_AND_MATCHING.md)) · microservices, multi-region, sharding, event sourcing.

## 16. Working method

Each milestone is decomposed with [agent/planner.md](../agent/planner.md) into a bounded task (goal, allowed files, tests, acceptance) before code. High-risk surfaces that _do_ appear in the MVP — authorization, tenancy/`access_grant`, migrations, immutable evidence — still get an [agent/security.md](../agent/security.md) design check, because those are the parts we are _not_ deferring. Everything deferred to the hardening gate is flagged, not silently skipped.

## 17. Security review record

Reviews follow [`agent/security.md`](../agent/security.md). This section records what was reviewed, what was found, and what remains open, so `verification` is a queue with a visible exit rather than an indefinite parking state.

### 2026-08-29 — A1b, A2, A3, B4, B5

**Scope:** session/workspace adapters and revocation; the OIDC authorization-code + PKCE boundary and browser cookie transport; the publish transaction; the public discovery read path. Reviewed against `60_API_CONTRACT`, `70_SECURITY_AND_AUTHZ`, and the `AGENTS.md` load-bearing constraints.

**Findings**

| #   | Severity                 | Location                                                       | Finding                                                                                                                                                                                                                                                                                                                                                                                                                                      | State                                                                                                                                                           |
| --- | ------------------------ | -------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Medium                   | `compose.yaml` (postgres service)                              | The `postgres` image was tag-pinned only while `dex` was digest-pinned, so "exact Compose pin" (A1a) was not met and the tag could move under the stack.                                                                                                                                                                                                                                                                                     | **Fixed** — digest added.                                                                                                                                       |
| 2   | Medium                   | `compose.yaml` (api + web healthchecks)                        | Both healthchecks called `fetch` without consuming or cancelling the response body, so the process lingered on undici's keep-alive socket for ~8.2s against a 3s timeout. `api` could never become healthy and `web` — gated on `service_healthy` — never started. Measured 8.21s → 0.10s with the body cancelled.                                                                                                                           | **Fixed** — body cancelled; full stack verified healthy.                                                                                                        |
| 3   | Medium                   | `apps/api/src/app.ts` (public routes)                          | The unauthenticated discovery endpoints have no abuse or rate controls. `agent/security.md` requires them for public endpoints.                                                                                                                                                                                                                                                                                                              | **Open** — gate G (§9), API-wide rather than B5-specific.                                                                                                       |
| 4   | ~~Medium~~ **Withdrawn** | `packages/contracts/src/challenge.ts` (`PublishChallengeBody`) | Originally raised as "publish requires no structured reason". This misread `70` §4: that row's `✔*` described the unimplemented **override** path, not routine publication ([95 §2](95_RISKS_AND_OPEN_QUESTIONS.md)). On the routine path the four gate reasons already carry the justification, and a mandatory free-text field there would dilute the audit trail rather than enrich it.                                                  | **Withdrawn 2026-08-29** — no change to `PublishChallengeBody`; §4 split instead.                                                                               |
| 5   | Low (doc)                | `70_SECURITY_AND_AUTHZ` §4                                     | Not a contradiction with `20_CANONICAL_MODEL` §8 after all: one matrix row conflated routine publication (`org:publisher`) with the override (`org:publisher` + `platform:ops`, step-up + reason). `platform:ops` co-signs an override; it never publishes in an organization's place.                                                                                                                                                       | **Fixed 2026-08-29** — §4 split into `challenge:publish` and `challenge:publish-override`; override tracked under B6, gated on [DEC-2026-012](25_DECISIONS.md). |
| 6   | Low (residual)           | `apps/api/src/browser-session.ts`                              | The browser flow cookie carries an unsigned `state`/`codeVerifier`. Forged flows cannot succeed — `state` is HMAC-derived from `OIDC_FLOW_SECRET` and matched against a server-side `oidc_authorization_attempt` digest — but an attacker able to _write a cookie on the origin_ could inject their own legitimate flow and log the victim in as themselves (login CSRF). Requires XSS, a cookie-writing sibling subdomain, or an HTTP MITM. | **Accepted residual** — revisit at gate G alongside cookie scoping.                                                                                             |

**Positive controls verified (evidence, not assertion)**

- Every response carries `cache-control: no-store` from a global `onRequest` hook — confirmed against the running container, so the cookie-varying public response cannot be cached and served across audiences.
- Public projection isolation proven by renaming `challenge`/`challenge_version`/`challenge_approval` out of reach and re-running both public reads.
- Non-enumerating `NOT_FOUND` confirmed live on the containerized API for an unknown challenge id.
- Session revocation denies immediately — proven in a real browser against the containerized stack.
- PKCE `S256` support is required at discovery; `state`, `code_verifier`, and `nonce` are HMAC-derived and stored only as digests; the token endpoint uses `none` auth with PKCE.
- Session cookies are `HttpOnly`, `SameSite=Lax`, and `Secure` whenever the origin is HTTPS; HTTPS is enforced in production and HTTP origins are restricted to loopback.
- Money is stored as integer minor units with an explicit currency; no float path exists in the publish or projection write.

**Untested boundaries.** No penetration test, no load or abuse testing, and no multi-tenant production dataset. Findings 3–5 are unremediated by design (owner decision or gate G).

**Reviewer independence.** B4 and B5 were reviewed by the same agent that implemented them. That is weaker than an independent pass and should not be treated as equivalent; A1b, A2, and A3 were reviewed as pre-existing code.

**Verdict:** pass for A1b, A2, A3, B4, B5.

**Amendment 2026-08-29 (same day).** Findings 4 and 5 were re-examined against [95 §2](95_RISKS_AND_OPEN_QUESTIONS.md) and both dissolved: `70` §4 had merged two distinct operations into one `challenge:publish` row, and the step-up/reason requirement belonged to the override rather than to routine publication. Finding 4 is withdrawn; finding 5 is reclassified as the documentation defect it actually was, and fixed. B4 moves to `done` with no code change. What the re-reading _did_ surface is a genuinely unscheduled capability — the publication override — now tracked under B6 and blocked on [DEC-2026-012](25_DECISIONS.md).

This is the third documentation-drift defect found by hand in two sessions (the stale worker allowlist, the closed-but-still-listed FR-ORG-004 gap, and this merged permission row). The last one nearly caused a wrong API change. An automated drift check in the shape of `check-canonical-vocabulary.mjs` would have caught at least two of the three.

### 2026-08-30 — B7, B8 closure

**Scope:** the connected governance page, public challenge view, role-scoped platform approval queue, and allowlisted platform approval brief. Reviewed against `60_API_CONTRACT`, `70_SECURITY_AND_AUTHZ`, and the `AGENTS.md` load-bearing constraints.

**Findings**

| #   | Severity       | Location                                                          | Finding                                                                                                                                                                                                                                                                          | State                                                                                                                                                                                                        |
| --- | -------------- | ----------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 7   | Medium         | `apps/api/src/app.ts`, `apps/api/src/postgres/challenges.ts`      | B8a originally handed a platform gate approver the entire private aggregate, including contact PII and private file identifiers.                                                                                                                                                 | **Fixed** — the org route is org-only again; a dedicated approval-brief route selects an explicit SQL allowlist and its closed schema excludes contact, invitees, attachment ids, tenant id, and creator id. |
| 8   | Low            | platform approval evidence                                        | The original read exposed `approvals[].recorded_by` user ids across tenants.                                                                                                                                                                                                     | **Fixed** — the platform summary exposes role, decision, reason, time, and `recorded_by_current_actor`; it never returns another user's stable id.                                                           |
| 9   | Low (residual) | `apps/api/seeds/a1a-synthetic.sql`, `infra/local/dex/config.yaml` | The five B7 governance identities share one bcrypt hash and are seeded unconditionally by `database-setup`. Harmless locally (`@synthetic.invalid` is unroutable, the stack refuses non-loopback), but the seed has no guard preventing it running against a non-local database. | **Accepted residual** — same exposure as the pre-existing seeds; gate G should add an explicit environment guard for all of them, not just these.                                                            |

**Positive controls verified (evidence, not assertion)**

- The platform brief is scoped to the `approvals` stage and to roles derived from `gateApproverRoles`; a draft, an unknown id, and a foreign org all answer the same non-enumerating `NOT_FOUND`.
- The queue is authorized from an active platform workspace, derives the one gate from the active role, excludes already-recorded/self-recorded versions, and is bounded to 50 rows.
- The authority path is passed explicitly rather than inferred from `access.workspace.kind`, which is `"org"` on _both_ routes — the first implementation inferred it and silently skipped the stage restriction; a test caught it.
- The public challenge view renders only allowlisted projection fields, proven in a real browser by asserting five confidential values are absent from the DOM.
- Publishing is refused for the org owner (403) and for an under-approved version (409), and one actor cannot record a second gate on a version — all proven end to end.
- The governance gateway never infers a gate outcome locally: a refused command returns the server's typed error and does not reload or mutate local state.

**Untested boundaries.** No penetration test or abuse/rate testing. The local synthetic-identity seed guard remains a pre-pilot item (finding 9).

**Reviewer independence.** All of this code was written by the same agent that reviewed it. That is weaker than an independent pass and should not be treated as equivalent — findings 7 and 8 are exactly the kind an independent reviewer is likelier to catch first.

**Verdict:** pass. Findings 7 and 8 are closed; B7/B8 are proven through rebuilt Docker images and visible browser actions.

### 2026-08-30 — B6

**Scope:** migration `0009`, the `extendDeadline` and `changePublicationState` commands in both adapters, and the discovery filter that follows from them. High-risk triggers: migration, authorization, public projection.

**Findings**

| #   | Severity | Location                                                     | Finding                                                                                                                                                                                                                                                                                                                                                        | State                                                                                                                                                                            |
| --- | -------- | ------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 10  | Medium   | `apps/api/src/{in-memory-challenges,postgres/challenges}.ts` | Both lifecycle commands validated stage and state but **not `context.role`**, relying entirely on `app.ts` — a defence-in-depth regression against the pattern B2's gate recording and B4's publish both follow. A future caller reaching the port directly (a worker, a script, a second transport) would have paused or cancelled a live call with any role. | **Fixed** — adapter-level `org:publisher` check in both commands, with a test asserting `org:owner`, `org:member` and `platform:ops` are refused 403 and the state is unchanged. |
| 11  | Low      | `apps/api/src/postgres/challenges.ts` (projection updates)   | The projection `UPDATE`s match on `challenge_id` alone. Safe today — the challenge was already resolved through a tenant/workspace-scoped `findScoped` in the same transaction, and `challenge_id` is the primary key — but it is the one statement in the slice that does not carry its own scope.                                                            | **Accepted** — add `tenant_id` to the predicate when the projection next changes shape; recorded so it is a choice rather than an oversight.                                     |

**Positive controls verified (evidence, not assertion)**

- The approved version and its `eligibility_rule` are never written by either command; a test asserts `published_version_id` and `content.proposal_deadline` are unchanged after an extension.
- Deadlines move forward only, refused by the command (422) _and_ by a database trigger (23514), so a direct SQL write cannot shorten a deadline solvers already saw.
- `closed`/`cancelled` are terminal in the command (409) and in the database (23514).
- A paused call leaves discovery but keeps its row, so a direct link still resolves and shows `state` — proven against PostgreSQL.
- A forced mid-commit failure leaves publication state, deadline, projection state, audit and outbox all unchanged.
- Every emitted lifecycle event stays inside the worker's supported set, so none can be silently dead-lettered.

**Untested boundaries.** No penetration or abuse testing. The acceptance clause about in-flight proposals staying readable cannot be tested until Phase 3 creates proposals. Material amendment and `challenge:publish-override` are deferred, not reviewed.

**Reviewer independence.** Self-review of code written in the same session — weaker than an independent pass. Finding 10 is a good illustration: it is a pattern deviation an independent reviewer familiar with B2/B4 would likely have caught faster.

**Verdict:** pass, with finding 10 fixed in the same change and finding 11 accepted.

### 2026-08-31 — Phase-2 connected UI closure

**Scope:** B5 public catalogue/detail and B6 publisher live-call controls. Reviewed against the public-projection, browser-authority, authorization, idempotency, versioning, and non-enumeration constraints in `AGENTS.md`, `60_API_CONTRACT`, and `70_SECURITY_AND_AUTHZ`.

**Finding:** the connected catalogue itself used only B5 projections, but a person who directly entered a pre-generated fixture detail URL could still see demo content in network mode. This did not disclose private data, but it blurred the production/demo authority boundary. **Fixed:** connected builds now return 404 for legacy fixture detail paths; only `/challenges/record?id=chl_…` resolves server records. The Docker B7 test asserts this behavior.

**Positive controls verified:** catalogue cards are built only from `ChallengePublicProjectionResource`; the public API decides anonymous versus registered visibility from the session; lifecycle controls are UX only and every action still reaches the `org:publisher`-authorized, versioned, reasoned, idempotent server command; terminal close/cancel require confirmation; server time remains authoritative for deadline validity. The static demo retains its fixture catalogue and excludes the connected B5/B6 components from its JavaScript bundle.

**Residuals:** public-endpoint abuse controls remain finding 3 / gate G; free-text search and solver-specific fit wait for Phase 3. This was a self-review, not an independent security assessment.

**Verdict:** pass after closing the connected fixture route. No new unresolved security finding.

### 2026-08-31 — Phase-2 review closure

**Scope:** the external Phase-2 review covering rejection recovery, ops triage, eligibility snapshots, deadline expiry, projection integrity, pagination/filtering, synthetic credentials, reset ergonomics, and purpose-scoped platform access.

**Closed in code and tests:** a rejected gate can be reworked only through a new immutable formulation version; rejection is available in the governance UI; rough-brief triage and full formulation readiness are separate; ops has a visible cross-tenant triage action whose brief disappears after success; expiry is derived from server time; autosaves no longer create eligibility rules; publication requires the governed snapshot and a future deadline; public cursor/category behavior is stable and normalized; projection inserts as well as updates must match the aggregate; platform brief fields are purpose/gate-specific; shipped synthetic credential helpers are removed; local database commands share `.env`, and a guarded full reset is explicit.

**Clarified, not expanded:** `published` is the delivered canonical lifecycle stage. B6 `open/paused/closed/cancelled` values are the live-call substate; the later `published → evaluating` canonical transition belongs to Phase 3 once proposals exist. The immutable rule deadline records what was approved; the aggregate deadline records whether the call currently accepts proposals, so C1/C4 must consult both. Organization disclosure on public records remains an owner policy decision rather than an inferred projection field. `GET /challenges/{id}/versions` is marked future until an endpoint exists.

**Verification boundary:** this closure is self-reviewed. Production RLS, managed OIDC/MFA, public abuse controls, private files, and independent security review remain gate-G work; none is being represented as complete by Phase 2.

### 2026-09-02 — C1 solver facts and eligibility

**Scope:** migration `0014`, solver profile/verification and gate-acceptance adapters, six C1 API operations, eligibility evaluation, outbox routing, and their domain/contract/API/PostgreSQL tests. Reviewed against `60_API_CONTRACT`, `70_SECURITY_AND_AUTHZ`, DEC-2026-016, and the `AGENTS.md` authority, tenancy, immutable-evidence, and atomic-write constraints.

**Findings:** no unresolved Critical, High, Medium, or Low code finding. The review did require normalized-array validation at the API boundary so inputs rejected by PostgreSQL fail as typed `VALIDATION`, and a wrong-role API/adapter test was added for `team:viewer`.

**Positive controls verified (evidence, not assertion)**

- Every protected operation resolves the active workspace through `runAuthorizedWorkspace`; session, membership, workspace kind, and role are revalidated before the scoped adapter call. Suspended membership, revoked session, wrong role, and cross-workspace ID swap have negative API coverage.
- PostgreSQL reads predicate on solver `(tenant_id, workspace_id)` before joining challenge data. Eligibility reach starts from the public projection and returns the same `NOT_FOUND` for unknown, unpublished, out-of-reach, or scope-mismatched records.
- Workspace verification is a separate record; verified OIDC contact never advances it, and the solver-facing command permits only `not_started → draft`.
- Gate acknowledgement is bound to the current published challenge version and required rule flag, records the accepting user, and is append-only. The synthetic document acknowledgement carries no file/evidence claim.
- Profile, verification-start, and gate writes bind idempotency to tenant, actor, workspace, action, target and body; optimistic version/expected-zero checks and mutation/audit/receipt/outbox/idempotency evidence share one transaction. Outbox payloads contain only stable IDs, versions, and gate code.
- Eligibility is deterministic server code over the exact rule, aggregate live state/deadline, workspace facts, and one server timestamp; no browser state or model participates.

**Residual risks / untested boundaries:** the owner subsequently executed the focused PostgreSQL and connected-boundary suites against the local Docker stack and returned review refinements on the Phase-3 branch. C1 remains `verification` pending independent assurance and the later connected-flow gates. Production RLS, managed verification-provider evidence, private file scanning, rate controls, and independently operated audit remain gate-G/C7 work and are not claimed here.

**Reviewer independence:** implementation was delegated to the implementer agent and reviewed by the primary agent, but the primary also made small validation/test refinements. Treat this as a same-session review, not external assurance.

**Verdict:** pass at the reviewed code/contract boundary and owner-run local PostgreSQL boundary; independent assurance and later connected-flow gates remain before `done`.

### 2026-09-02 — C2 authoritative team lifecycle

**Scope:** migration `0015`, the canonical team permission matrix, team policy/invitation/request/membership/ownership/archive adapters, typed routes/OpenAPI, worker event allowlist, and domain/contract/API/PostgreSQL tests. Reviewed against `20_CANONICAL_MODEL`, `60_API_CONTRACT`, `70_SECURITY_AND_AUTHZ`, DEC-2026-016, and the `AGENTS.md` tenant, authority, immutable-evidence, concurrency, and atomic-write constraints.

**Findings closed during review:** verified-primary-email binding is required before an invitation can bind to a user; invitation acceptance now acquires the team aggregate lock before invitation/member locks; whitespace-only structured reasons and bounded team text fail as typed validation; self-removal is routed through leave; policy and permission definitions were centralized in `@rahhal/domain`; and same-tenant foreign member IDs receive the same non-enumerating denial as other out-of-scope IDs.

**Positive controls verified (evidence, not assertion)**

- Team creation produces a separate team workspace, owner membership, canonical `TeamKind` profile, and `not_started` workspace verification without creating another credential.
- Manager operations start from the active tenant/team workspace and current membership, then apply the shared five-role/eight-policy decision matrix; proposal-manager profile edits require the durable policy flag, while verification and gate acknowledgement remain owner/admin operations.
- Invitation and request commands bind to the authenticated verified recipient/requester. Suspended, removed, left, or archived memberships immediately stop authorizing access; auditable rows are retained.
- Membership-count changes lock the team aggregate before member rows. Ownership transfer changes both roles and `workspace.owner_user_id` atomically, and deferred constraints require exactly one matching active owner at commit.
- Every sensitive mutation carries idempotency and expected-version semantics and writes the aggregate change, receipt, audit event, and allowlisted outbox event in one transaction. Structured reasons stay in audit metadata and are excluded from outbox payloads.
- Database triggers protect team identity, terminal invitation/request evidence, membership identity/version history, and archived teams from destructive or invalid transitions.

**Residual risks / untested boundaries:** the owner subsequently exercised the PostgreSQL/Docker boundary and returned two refinements now present on the branch: non-team membership deletes no longer bypass the lifecycle guard, and deferred operation denials are durably audited outside the rolled-back authorized transaction. C8 notification projection, C9 connected team UI, managed MFA/step-up, RLS, abuse controls, independent security assurance, and independently operated audit remain separate gates and are not claimed here.

**Reviewer independence:** the implementer agent contributed the initial implementation but reached its execution quota; the primary agent completed and reviewed the boundary, including validation, concurrency, negative authorization, and documentation refinements. The owner's later Docker inspection is separate review evidence, but not a production security assessment.

**Verdict:** pass at the native and owner-run PostgreSQL/Docker boundary; C2 remains `verification` pending independent security assurance and C8/C9 connected-flow gates.

### 2026-09-03 — C3 authoritative proposal drafts

**Scope:** C3 proposal content/readiness contracts, scoped draft create/read/save adapters, Fastify/OpenAPI routes, metadata-only proposal events, and domain/contract/API/PostgreSQL tests over the existing migration `0013` proposal foundation. Reviewed against `20_CANONICAL_MODEL`, `60_API_CONTRACT`, `70_SECURITY_AND_AUTHZ`, and the `AGENTS.md` tenant, current-authority, immutable-version, atomic-evidence, concurrency, and confidential-data constraints.

**Findings closed during review:** the C2 refresh initially exposed merge-side removal of five existing challenge denial-audit blocks; those blocks were restored before C3 verification. The proposal version-history query now repeats the owning tenant/workspace predicate instead of relying only on an already-scoped parent ID. The PostgreSQL fixture now creates the registered public projection C3 actually requires, and the suite covers concurrent different-key creation plus same-version save races. The refreshed A1b denial-audit test also receives its branded tenant ID without weakening the user-authored regression.

**Positive controls verified (evidence, not assertion)**

- All proposal routes revalidate the active session, solver workspace, and membership in the existing workspace authority before reaching the proposal adapter. Organization/platform contexts fail closed; current role/team policy is rechecked before idempotent replay.
- Draft creation resolves only a separate `public`/`registered` projection and independently locks/checks the published challenge aggregate's current open/unexpired state. C3 does not treat verification, profile readiness, or declarations as a draft gate; C4 owns the atomic submission decision.
- Private aggregate and version-history reads predicate on the active `(tenant_id, owner_workspace_id)` before the proposal ID. Team access then uses the canonical create/edit decision and current proposal assignment; cross-workspace guesses, viewers, and unassigned contributors return non-enumerating `NOT_FOUND` for private drafts.
- Every save inserts an unlocked append-only `proposal_version` with the exact current base, computed changed fields and SHA-256 content hash, then advances the aggregate pointer/lock version under row lock. The database foundation independently enforces gap-free/current-pointer and append-only evidence invariants.
- Create/save evidence uses one PostgreSQL unit of work for aggregate/version, receipt, audit, outbox, and idempotency cache. Natural-key/advisory and aggregate-row locks serialize competing creates and saves. Event/audit payloads contain only stable entity/version metadata, never proposal content or filenames.
- Money is integer safe minor units plus an approved currency. `attachment_ids` accept only opaque `fil_*` metadata references; there is no upload, object lookup, scan claim, signed read, or file content in this milestone.

**Residual risks / untested boundaries:** the native non-Docker typecheck and domain/contract/API/worker suites pass, and the reserved Docker verification has since been executed (see the 2026-09-03 C3 Docker verification entry below). The live route check seeded the challenge publication through direct SQL rather than driving the B2 gate/publish routes, so publication itself is not covered by that run. C4 eligibility re-evaluation/locked submission/grant, C5 clarification/revision, C8 notifications, C9 connected UI, G3 private-file authority, production RLS/rate limits, and independent audit remain separate gates.

**Reviewer independence:** the implementer agent contributed the initial implementation and focused tests, then reached its execution quota. The primary agent completed merge reconciliation, database-query scoping, concurrency coverage, documentation, and the security pass. Treat this as same-session review pending the owner's Docker inspection.

### 2026-09-03 — C3 Docker verification and save-race conflict fix

**Scope:** executing the reserved C3 PostgreSQL suite and a live route check against the Docker stack, plus the defects that execution exposed. Commit `c44c7d9`.

**Defect found and fixed:** `findScoped` locked the proposal row and joined `proposal_version` in one statement. Under READ COMMITTED a concurrent save advances `current_version_id`, EvalPlanQual re-checks the updated proposal row, and the version row it now points at was inserted after that statement's snapshot and is invisible to it, so the join returned zero rows. The loser of a save race was told the proposal does not exist: `404 NOT_FOUND` instead of `409 CONFLICT`, no `refetch_and_retry` recovery hint, and a denial audited as `record_unreachable` rather than a version conflict. Confirmed by probe, not inference: `findScoped` returned null while a later statement in the same transaction saw the row at `lock_version=2` with both versions visible. No data was at risk; the winning write and the gap-free version chain were always intact. The row lock now runs in its own statement and the joined content is read after it, so the follow-up statement sees the committed winner and reports the stale `expected_version` as `CONFLICT`.

**Suite defects fixed:** the authored suite could not run. `STALE_VERSION` and `IDEMPOTENCY_CONFLICT` are not among the seven codes in `apiErrorCodes`; both helpers report `CONFLICT`. `beforeEach` deleted from append-only `audit_event`, and tried to reopen the challenge closed by the closed-call behavior, which `challenge_publication_lifecycle_guard` forbids because terminal states stay terminal. Both are replaced by the full migrate/reseed that `postgres-identity-workspace.test.ts` already uses. No guard was disabled or weakened to make a behavior pass.

**Evidence:** C3 PostgreSQL suite 5/5, previously 0/5; full PostgreSQL suite 97/97 across 9 files; live route check 29/29 over HTTP covering scope, role denials, idempotent replay, version chain, validation, individual/team paths, and the save race; API 87/87; contracts 11/11; typecheck, lint, format; `docker:smoke` with restart persistence. Proposal outbox payloads carry only `{"entity_version": N}`, and every route denial recorded an `audit_event`.

**Still open:** owner and independent review. Publication was seeded via SQL rather than driven through the B2 gate/publish routes.

### 2026-09-04 — C4 Docker verification, migration repair, and foundation-test port

**Scope:** executing the reserved C4 PostgreSQL suite and a live route check against the Docker stack, plus the defects that execution exposed.

**Blocker found and fixed:** migration `0016` could not apply. `validate_c4_proposal_access_grant` declared `proposal_row proposal%ROWTYPE` and used it inside a multiple-item `INTO` list, which PL/pgSQL rejects with `record variable cannot be part of multiple-item INTO list`. `runMigrations` therefore failed for every suite: 47 tests across 8 of the 9 PostgreSQL files, including A1b, C2, and C3 behaviors that passed before C4. No C4 behavior had ever run against a database. The five columns are now selected as scalars.

**Foundation behaviors ported:** `0016` deliberately makes version locking insert-only — `protect_proposal_version_evidence` rejects every update and delete, as `50_DATA_MODEL` records. Three C3-era foundation behaviors still locked a version by `UPDATE` and so asserted the replaced contract. They now append the locked version the way `submit` does. The guard became stricter, not weaker; no invariant was relaxed to make a behavior pass.

**Gate repaired:** C4 committed two unformatted documents, breaking `npm run format:check` in the documented release bundle. Verified clean at `133885d` and failing at `9bdb2de`.

**Evidence:** PostgreSQL 103/103 across 9 files, previously 47 failing; live route check 22/22 covering exact-terms acceptance, server-side eligibility re-evaluation, readiness gating, tracking code, the `1->2->3L` version chain with only the submitted version locked, idempotent replay, refusal to re-submit or edit after submission, and the organization inbox/detail scope; API 90/90; contracts 12/12; typecheck, lint, format; `docker:smoke` with restart persistence. The submitted outbox payload carries only `grant_id`, `entity_version`, and `submitted_version_id`; the organization inbox carries no content and detail returns no draft history.

**Open observations (not changed):** `GET /organization/proposals` applies a hardcoded `LIMIT 100` with no pagination, so a larger inbox truncates silently. The organization routes guard on workspace kind alone, so every organization role — `org:approver_technical` included — reads full submitted proposal content with no conflict-of-interest declaration; reviewer COI belongs to a later phase and C4 claims no narrowing, but the boundary deserves an explicit decision before reviewers exist. The 30-day grant expiry remains an unratified default.

**Verification gaps:** the live check seeded the challenge publication and the solver's verification/gate acceptances through direct SQL rather than driving the B2 publication and C1 gate routes, so those paths are not covered by this run.

### 2026-09-04 — C5 review: lifecycle verified live, two defects fixed

**Scope:** reviewing C5 against the running stack — the full `submitted` through `resubmitted` lifecycle over HTTP, the PostgreSQL suite, and the static gates.

**Verified as specified:** all eight commands drive the lifecycle end to end. Clarifications are separate immutable evidence that never touch proposal content — the current version is unchanged across a full request/submit/resolve round, and the clarification retains question, response, and resolution. A revision appends an unlocked exact-base copy, becomes editable again, and resubmission appends a locked version whose `changed_fields` are diffed against the requested locked base (`["roadmap","budget_amount_minor"]` for a two-field revision, correctly excluding untouched fields). The version chain reads `1->2L->3->4->5L` with only the submitted and resubmitted versions locked. Resubmission revokes the superseded grant and creates the exact-version replacement in the same transaction, and the organization then reads the new version and its revised content. Wrong-state and stale-version commands are refused.

**Defect: a rejected proposal could not be recorded.** `decideEligibility` passed an empty `next_actions` for the `ineligible` branch, and `mutation_receipt_next_actions_check` requires a non-empty array. Every rejection therefore failed with a database error surfaced as `503 STORAGE`, rolling back with the proposal stuck in `eligibility_review` and no record of the attempt. The receipt schema permits an empty array while the database does not, so nothing caught the mismatch, and the suite covered only the `eligible` branch. `ineligible` is terminal in the canonical table, so the decision now reports `closed`, mirroring the challenge lifecycle's own terminal marker. A PostgreSQL regression covers the branch.

**Defect: the documented local reset became impossible.** `0017`'s down migration refuses to discard clarification and revision evidence, which is right for a real rollback. But `npm run db:reset` was built on the down migrations, so once any database had exercised C5 it could no longer be reset — the exact database a developer needs to reset. Reset now recreates the schema; it was already restricted to a non-production loopback host, and the evidence guard still governs every real `db:migrate:down`.

**Divergence recorded, not changed:** the five organization routes guard on `canReadChallenge`, which tests workspace kind only. The canonical transition table gives eligibility decisions `roles: ["org:member", "platform:ops"]`, so the routes are both broader and narrower than the model: `org:owner` drove every organization command in the live run without appearing in any of those lists, while `platform:ops` was refused `403 NO_ACCESS` although the table allows it. B2 already shows the shape a fix would take, with per-gate `isGateApproverRole` plus standing platform roles. Whether C5 intends to defer the platform path is an owner decision, so this is reported rather than implemented.

**Evidence:** PostgreSQL 106/106 across 9 files; live lifecycle review 27/28, the miss being the recorded `platform:ops` divergence; API 92/92; contracts 13/13; typecheck, lint, format; `docker:smoke` with restart persistence.

**Verification gaps:** as with C4, the live review seeded the challenge publication and the solver's verification and gate acceptances through direct SQL rather than driving the B2 and C1 routes.

### 2026-09-04 — C6 completion and review: three defects fixed

**Scope:** finishing the C6 slice, then reviewing it against the running stack — saved opportunities, direct offers, offer responses, and negotiation over HTTP, plus the PostgreSQL suite and the static gates.

**Defect: an offer to an already-collaborating workspace failed outright.** `0001` allowed one active `access_grant` per grantor/grantee/resource/capability, a rule written when every grant was standing. Sending an offer inserts its own challenge read grant, so a recipient that already held a standing grant for the same challenge collided on that unique index and the send failed with `503 STORAGE`. The seeded cross-tenant collaboration grant is exactly this shape, so the path is production-reachable, not a fixture artifact. `0018` now scopes the rule to standing grants and lets an offer-scoped grant sit beside one; `direct_offer_one_active_target_idx` already bounds live offers to one per challenge and recipient, and offer grants are revoked with their offer. The down migration restores the original index.

**Defect: deleting an unrelated access grant was silently cancelled.** `validate_c6_offer_access_grant` raised only for offer grants and then fell through to `RETURN NEW`. `NEW` is NULL on a DELETE, and a BEFORE DELETE trigger returning NULL cancels the statement silently, so removing any non-offer `access_grant` reported `DELETE 0` with the row still present and no error. This repeats the C2 membership defect fixed in `2188d11`; `0016` avoids it correctly with `RETURN OLD`, and `0018` now does the same. A PostgreSQL regression covers both halves.

**Defect: offer grants were judged against a different clock than the one that writes them.** The eligibility evaluator's new grant-visibility predicates used `transaction_timestamp()` while C6 writes `valid_from` from the injected clock, which it uses 14 times and `transaction_timestamp()` never. A grant could therefore be written outside the window the reader judges it by. Both predicates are now bound to the same injected clock the rest of that adapter already uses.

**Verified as specified:** save and unsave; an organization sending an offer bound to the exact published version; the recipient inbox; view, response draft, edit, and submit; the organization reading the submitted response and starting negotiation; money preserved as integer minor units with its currency; and cancellation closing recipient access immediately. Scope holds in every direction tested: an unrelated solver workspace neither lists nor reads the offer, an unrelated organization cannot read it, and an organization role without `manageDirectOffers` cannot send.

**Worth recording as an improvement:** C6 guards its organization routes with `organizationCapabilities(role).manageDirectOffers`, whose `["org:owner", "org:member"]` matches the canonical `organizationOfferManagers` exactly. That closes, for C6, the class of divergence recorded against C4/C5, where organization routes authorized on workspace kind alone.

**Also changed:** `domain/state-machines.ts` now re-exports the canonical `directOfferTransitions` instead of redefining them, deleting 90 lines of duplicate lifecycle. The canonical machine additionally requires `not-expired` on `received → viewed`, so a frontend behavior asserting the weaker replaced contract was aligned. C6's offer-response readiness messages were English on a Persian-first product and are now Persian, matching C3's proposal readiness.

**Evidence:** PostgreSQL 112/112 across 10 files; native suite 464/464 across 63 files; API 96/96; contracts 14/14; live route review 21/21; typecheck, lint, format, workspace boundaries; `docker:smoke` with restart persistence.

**Verification gaps:** as with C4 and C5, the live review seeded the challenge publication and the solver's verification and gate acceptances through direct SQL rather than driving the B2 and C1 routes.

### 2026-09-05 — C8 implemented: durable projection and the notification read model

**Scope:** building C8 from nothing — the notification read model, its commands, the canonical event-to-audience routing table, and the durable worker path the projection needs. The owner chose the full slice over projecting inside the API transaction, so the worker moves from the in-memory skeleton to PostgreSQL-backed claims.

**What landed:** migration `0020` adds `notification` and `outbox_delivery`. The worker gains `PostgresOutboxSource` (claims with `FOR UPDATE SKIP LOCKED`, a lease so a dead worker releases its rows, retry and dead-letter that keep the row inspectable rather than deleting it) and `PostgresDeliveryLedger`, which inserts the delivery record and runs the projection in one transaction. `OutboxHandler`/`DeliveryLedger` are parameterized over that transaction, so the in-memory path is unchanged and the PostgreSQL path can be atomic. `notificationProjections` in the domain is the exact event-to-audience table: an event absent from it produces no notification, because silence is the safer default.

**Confidentiality:** the projector resolves recipients by reading the aggregate inside the delivery transaction, never from the event payload, which carries only identifiers and versions. A notification stores kind, subject reference, and timestamps — no proposal or response content, no clarification or revision text, no contact details, no credentials. A client follows `subject_type`/`subject_id` back through the normal authorized read, so a deep link is re-authorized at the moment it is used rather than minted when the row was written.

**Defect found and fixed during the live review:** `PostgresOutboxSource.claim` compared `available_at` against the worker's own wall clock while rows stamp `available_at` from the database's `clock_timestamp()`. A freshly written event was unclaimable until the two clocks agreed, so any skew between the API host and the database silently delayed delivery. It surfaced as an intermittent test that passed whenever a probe added latency. Availability and the lease are now judged by `transaction_timestamp()`, matching the rule C4 and C5 already follow.

**Second defect fixed:** the notification command routes did not escape the literal action colon, so Fastify parsed `:read` in `/notifications/{id}:read` as a second route parameter and every mark-read returned `422 VALIDATION`. Both routes now use the `fastifyLiteralPath` helper the other command paths already use.

**Evidence:** PostgreSQL 122/122 across 12 files; native 473/473 across 64 files; API 103/103; contracts 15/15; worker suite passing; live review 14/14 covering projection, per-recipient scoping, foreign-organization refusal, idempotent mark-read, the unread filter, mark-all-read, and an unauthenticated refusal; typecheck, lint, format, workspace boundaries; `docker:smoke` with restart persistence.

**Still open:** owner and independent review. External email, SMS, and push delivery remain later work, and G4 still owns poison-isolation, WORM audit export, and correlation search. The compose worker now runs `RAHHAL_WORKER_MODE=postgres`; the in-memory mode remains for the demo runtime.

### 2026-09-07 — Leaving a workspace goes home, it does not ask where to

**Scope:** the owner reported that ending a team workspace dropped them on the workspace chooser.

**Why it asked.** Leaving or archiving the active team ends the context you were working in, and the server drops it — correctly, since it is no longer yours to be in. The page then fell through to `/app`'s resolver, which asks which workspace to enter. That question is right after signing in, where the choice is real. It is wrong here: you asked to leave, not to go somewhere, and a solver's personal workspace is the one they always hold and never leave, so there is exactly one answer.

**The change.** Both exits now enter the personal workspace themselves, reusing the pattern team creation already had — refresh identity, switch, let the page re-render. No navigation follows: `/app/solver/teams` is valid in either context, so the outcome stays visible on the page that produced it rather than flashing past on the way to a dashboard. The workspace id is read before the command runs, because it cannot be the one the command removed and `refreshMe` does not hand the new identity back synchronously.

**Evidence:** native 582/582; API 103/103; typecheck, lint, format; build, 519 routes, 520 link-checked files, smoke, offline, standalone-interactive; both budget sets. Verified live for both paths: archiving an owned team left the switcher holding only the personal workspace, and leaving someone else's team took it from three to two — each landing in the personal workspace with no chooser and no navigation.

### 2026-09-07 — `/me` lists the workspaces a human can enter, not every one they ever joined

**Scope:** the owner reported that leaving a team left it in the workspace switcher and in «تیم‌های من».

**It was the server, not the chrome.** `/me` built its `workspaces` list from every membership row regardless of state, and both the switcher and the team list are built from that list — so no amount of client refreshing could remove the team. The response was internally contradictory: it reported the membership as `removed` and the workspace as available in the same payload. Authority was never affected — entering the team answered `NOT_FOUND` — which is exactly the "looks live but isn't" failure C9 exists to remove: a door offered and then refused.

**The rule already existed.** `activeAccess`, the authorization path, resolves a workspace only when the membership is active and, for a team, the team workspace is not archived. `/me` now computes that same predicate and filters `workspaces` by it, so the two answer alike. `memberships` stays complete, because a removed membership is a true fact about that person and its `state` is what says so — which is also what keeps the team in «دعوت‌های پاسخ‌داده‌شده» after leaving. Both adapters were wrong in the same way and both are fixed, so the demo and connected runtimes still agree.

**Three states, one cause.** Leaving, being removed or suspended, and belonging to an archived team all left a workspace in the list; an archived team keeps its memberships active by design, so its former members were still offered it. The regression test walks all four transitions against PostgreSQL and fails against the unfiltered list.

**Evidence:** native 581/581; API 103/103; PostgreSQL 130/130; typecheck, lint, format, boundaries; build, 519 routes, 520 link-checked files, smoke, offline, standalone-interactive; both budget sets. Verified live: a contributor left a team, the resolver offered only the two workspaces still theirs, and the switcher and «تیم‌های من» agreed without a reload.

### 2026-09-07 — Both parties to a proposal read the same submission

**Scope:** D-07 from the end-to-end review, settled before Phase 4 opens, because D3 rubric scoring is defined against the content the organization can see.

**What was wrong.** The organization's record rendered eight of the thirty fields a solver submits, and none of the four declarations. The party that must decide on a proposal saw less of it than the party that wrote it: no mitigation plan, no relevant experience, no budget rationale, no technologies, no duration, and no way to confirm that the NDA, conflict, IP and accuracy declarations C4 had required before accepting the submission were actually made. Nothing was being withheld on purpose — the server returns the whole content to a granted organization — the two record pages had simply hand-listed their fields at different times and drifted.

**The fix is one definition, not two longer lists.** `lib/workspace/proposal-content-fields.ts` owns how a submitted proposal reads: eight groups (مسئله و ارزش، راهکار فنی، اجرا و زمان‌بندی، سنجش و ریسک، تیم، بودجه، مالکیت فکری و تعهدها، فایل‌ها), with the week counts and the money formatted in one place rather than two. Both records render from it, so they cannot come apart again, and `keyof ProposalContentResource` makes a new content field fail the build rather than appear on one page and not the other. A declaration reads as تأیید شده / تأیید نشده, because it is evidence about a term the challenge set, not a checkbox to interpret.

**Why every field.** There is no field a solver submits to an organization that the organization should not read — submission _is_ the disclosure. The one thing still withheld is the file behind an attachment id, which is G3 scope; the record says so rather than implying a file it cannot open.

**Evidence:** native 581/581 (a new `tests/proposal-content-fields.test.ts` asserts full contract coverage, the declarations specifically, and that both pages render from the shared definition — proven to fail both when a group is removed and when a page stops using it); API 103/103; PostgreSQL 129/129; typecheck, lint, format, boundaries; build, 519 routes, 520 link-checked files, smoke, offline, standalone-interactive; both budget sets, with the grouping styles costing 130 gzipped bytes against 25,043 of headroom. Verified live on both records, and checked for horizontal overflow at 1280 and at 375.

### 2026-09-07 — Phase-3 end-to-end review: every flow walked, five defects fixed

**Scope:** the owner had walked C10 and reported it "generally working", with edge cases and the less-travelled flows untested. This entry covers a systematic walk of every Phase-3 family against the running stack — identity and OTP edges, eligibility gates, draft concurrency, submission and immutability, the bilateral clarification/revision chain, the full team lifecycle, saved opportunities and direct offers, notification projection coverage, public-audience rules, live-call controls against submission, and cross-tenant isolation on every family — followed by a browser walk of both workspaces.

**The one that mattered.** `team.membership-request.rejected` projected zero notifications. The audience resolved the requester through a membership in the _team's_ workspace, which only an accepted requester ever holds, so a rejection reached nobody: the one person waiting on the answer was the one person never told. It is the same shape as the invitation defect fixed the day before, in the sibling branch of the same function, and it was invisible to 573 passing tests — the event published, the row count was zero. A requester now reads the decision in their own workspace while the row still names the team they asked to join.

**What the browser walk found.** Accepting a team invitation said "دعوت پذیرفته شد" above a team list that did not contain the team, until a full reload: the page refreshed the family read but not `/me`, which is what both the list and the workspace switcher are built from. The dashboard's action queue captioned an unsubmitted draft with 32 hex characters, the pattern `RecordReference` exists to prevent — the assertion that catches it did not list that file. The record's primary action read "اعمال اصلاحات" in every actionable state, so answering a clarification and revising a locked version were the same button under the same wrong name, while the editor behind it had always been state-aware. And `زمان اجرا` printed the stored string raw, putting a Latin `16` beside `نسخه ۵` and `۱٬۵۰۰٬۰۰۰ ریال` on one record.

**What held.** Everything else, and the negative paths are the part worth saying: a wrong OTP is refused without leaking the code and the attempt budget locks even against the correct one; two concurrent saves at one version produce exactly one winner; a submitted proposal is immutable to its own author; the clarification chain walks end to end with base-version citation and a real diff; a suspended member is denied on the next request and a sole owner cannot leave; a sender cannot read an unsubmitted response draft; pause and close both refuse submission with `call_not_open` and resume restores it; an owner cannot pause a live call, which is separation of duty working; an anonymous visitor cannot read a registered-only call while a signed-in solver can; and every cross-tenant, foreign-solver and unknown-id path answers `NOT_FOUND` without revealing whether the record exists.

**Left for the owner**, recorded with evidence rather than fixed here: the organization's record renders 8 of the proposal's 30 content fields, so the party that must decide cannot see the mitigation plan, the relevant experience, the budget rationale, the technologies, the duration, or the four declarations C4 required — a render gap, since the API already returns them, but a decision about what the reviewing party sees, and one Phase 4's rubric scoring will need settled. The organization's own challenge list labels every published call "منتشرشده" whether it is open, paused or closed. The OTP session exchange still answers `select_workspace` where the OIDC path answers `continue`. And the dashboard shows two counts with near-identical Persian wording for two deliberately different sets.

**Evidence:** native 573/573; API 103/103; PostgreSQL 129/129; typecheck, lint, format; build, 519 routes, 520 link-checked files, smoke, offline, standalone-interactive; both budget sets. Both new regressions were run against the original code first and fail there. All five fixes verified live in the browser after a stack rebuild.

### 2026-09-07 — The CSS budget stops being a change detector

**Scope:** the owner asked for the demo CSS budget to be resolved so frontend work stops tripping it. Three gates were failing; all three failed for the same reason, and raising them again would have guaranteed a fourth failure.

**Why it kept failing.** `staticAssets.maxCssBytes` had moved 503,436 → 513,000 → 516,000 → 520,000 inside a fortnight. `maxLargestCssBytes` stood 636 bytes under a build it then failed. `standalone.maxBytes` had 3,353 bytes of headroom and `maxJavaScriptCharacters` 1,882. A ceiling drawn within 0.1% of the last build is not a budget; it is a detector for whether the number changed, and the only remedy it offers an author is to edit the ceiling.

**And why it drained so fast.** The ceilings counted uncompressed bytes. Stylesheets are served compressed and this build's CSS gzips to 17.6% of source, so every rule was charged about six times what it costs a reader. `standalone.maxCssCharacters` measured something else again: the offline inliner rewrites every `url()` as a data URL, so roughly four fifths of that 2.5M count is base64 fonts and images — a font swap and a styling regression were indistinguishable in it.

**The change.** CSS is now enforced on gzipped transfer size (99,827 / 125,000 total, 39,429 / 50,000 largest), and every enforced ceiling in the demo and standalone sets keeps at least a fifth of itself in headroom against the build that sets it. The three uncompressed totals stay printed as trends against a reference, exactly as DEC-2026-014 treats total emitted JavaScript. The JavaScript route budgets DEC-2026-014 owns are untouched, and the network set was rebuilt and re-checked to confirm it.

**What the new ceiling actually allows.** Measured against the build that sets it, roughly 150 KB of further authored CSS fits — more than twice what six days of C9 stage 5 and solver-workspace styling added — while an accidentally duplicated `globals.css` (257 KB, 42,886 bytes on the wire) still fails it. That is the test: ordinary work fits, an accident does not.

**Evidence:** native 569/569; typecheck, lint, format; build, 519 routes, 520 link-checked files, offline, standalone-interactive; both demo and network budget sets pass. Headroom is asserted in `tests/performance-budget-checker.test.mjs` rather than left to the next person's judgement, and the retired keys are asserted absent so they cannot quietly return.

**Not done:** design-token consolidation is untouched — two competing custom-property systems still load on every page, and that work should reduce both the trend and the ceilings rather than spend this headroom. Payload reduction stays the G5 hardening item, and the DEC-2026-009 p75 targets still need real browser/RUM evidence.

### 2026-09-06 — C8 notification audit and the solver personal-workspace backend review

**Scope:** the owner reported that notifications "do not work as expected" without naming the symptom, and asked for a backend review of the solver's personal workspace alongside it. The symptom had to be found rather than guessed, so both were driven against the running stack.

**The notification defect: every team invitation reached nobody.** Comparing outbox events against the rows they produced showed one projection with a 100% drop rate: five `team.invitation.sent` events, five published deliveries, zero notifications — while all twelve other kinds projected. The recipient query resolved an active membership in the _team's_ workspace, which is precisely what an invitee does not hold yet; the join matched nothing every time. Two of those five invitations named a known, activated user with a personal workspace to read them in. An invitation now resolves the invitee's own individual workspace, while the notification's subject still names the team it is about — the row is scoped to where it is read and points at what it concerns. An invitation addressed to a contact that has never activated still notifies nobody: C8 reads no contact details, and that person is reached out of band.

**A badge that contradicted the page under it.** Marking a notification read cleared the row and the page count, but the header kept its old number. The commit announcement — the event a page fires to say a notification command just committed — was registered through the same visibility-gated handler as the 15-second poll, so it re-read only when `document.visibilityState` happened to be `visible`. The poll stays gated, because a background tab should stay quiet; the announcement does not, because it is not a poll.

**Presentation.** Every notification row printed a bare `wsp_…`/`prp_…` beneath its headline, which is the identifier problem the record chips already solved elsewhere; the row now uses `RecordId`, labelled by what the identifier belongs to. The solver sidebar gained the unread badge the organization sidebar already carried, so the same unread state is announced at the same volume in both personas.

**Solver personal workspace, backend.** Every route was walked with a freshly activated solver over real HTTP: activation, profile read and versioned patch, verification, proposals, saved opportunities, direct offers, incoming invitations and membership requests, notifications, and the public projection. Reads answer 200; team-space routes answer `NO_ACCESS` from a personal workspace; a stale `expected_version` answers `CONFLICT`; a workspace header that disagrees with the session's active context answers `NOT_FOUND` rather than revealing whether the record exists. The eligibility walls hold under a _complete_ draft, which is the case readiness alone cannot cover: a team-only call, an unverified identity, and unaccepted NDA/document gates each refuse submission with `INVALID_STATE`, and accepting both gates turns the same call eligible.

**The defect that walk found: a Persian keyboard could not submit a proposal.** Readiness tested the week counts with `\d`, which matches ASCII only, so `۸` and `۱۶` — the digits the product's own Persian keyboards produce — were reported as malformed with no hint that only Latin digits were accepted. Readiness is the one rule draft reads, preview and submit all share, so the whole submission was unreachable. It now normalizes Persian and Arabic-Indic digits the way the contact-verification code path already did. Normalization is not permission: a zero or non-numeric week count is still refused in either script.

**Evidence:** native 561/561; API 103/103; PostgreSQL 128/128; typecheck, lint, format, workspace boundaries; build, 519 routes, 520 link-checked files, smoke, offline, standalone-interactive; network budgets. Both new regressions were run against the original code first and fail there. Verified live in the browser: an invited solver's notification page shows the invitation, the identifier reads as a labelled chip, and marking it read clears the sidebar badge in the same interaction.

**Not done:** the demo CSS budget is over its ceiling from styling work in progress outside this change; it is left failing rather than absorbed, because moving the ceiling is a decision about that work. `direct-offer.declined`, `direct-offer.expired` and the three `team.membership-request.*` kinds have no events in the local data yet, so their projections are covered by the routing table and unit tests but not by a live walk.

### 2026-09-06 — C9 stage 5: browser review of both workspaces, and the defects it found

**Scope:** the owner walked the organization and solver surfaces in a real browser and reported what was wrong. This entry covers the review, the five phases of repair it produced, and the three questions it surfaced that the owner still has to answer.

**What the review found.** A live route was reading fixtures: the solver's connected proposal record rendered the demo repository's detail, so a server `prp_…` was never found there and every record answered "not found" **without asking the server at all**. Alongside it: the organization's help link pointed at its own settings page, the organization sidebar had no notifications entry, `/app/solver/invitations` and `/app/solver/teams` rendered one component under three different names, and the organization's action panel showed a bare heading and a raw `pcl_…` whenever a clarification had been answered.

**Identity.** Connected cards titled themselves with whichever identifier was present, so an unsubmitted draft was headed by 32 hex characters while the call it answers sat underneath in smaller type. `RecordReference` and `RecordId` settle it once: the human-meaningful line leads, the routing key becomes a copyable chip. Two defects came out of building it — the confirmation class was assembled by interpolation and produced `rh-record-idis-copied`, and the chip's `min-width: auto` floor outranked its `max-inline-size`, so a 64-character fingerprint spilled across the cells beside it (measured: 303px and 546px chips in 180px cells).

**Structure.** Both inboxes now group by the call each proposal answers, because proposals are only comparable within a call and an organization running several at once had to read the challenge line on every card. Groups order by work waiting, then recency. The version identity and content fingerprint moved off every list row into a summary rail on the record, beside the decision they make attributable. The solver side gained the same grouping, live status counts, a saved-profile read view, and a teams page that finally lists the teams a human belongs to.

**Sessions.** Signing in as a platform operator exposed four defects in one walk. Sign-out was wired to the organization alone, so an operator or reviewer with a real OIDC session could not end it. The operations shell captioned itself with a fixture person beside that operator's real publication queue. The session exchange left the active context null and always answered `select_workspace`, so every sign-in met a chooser — for a single-workspace human, a chooser with one button on it — and the first scoped request after signing in was refused. And the OIDC callback sent everyone to the organization's challenge form, landing a platform operator on a page they have no authority for. Both adapters now enter the one workspace a human holds and keep the choice open when it is real.

**CSS.** The enforced ceiling had been exceeded by 38,777 bytes. Measuring first found 46,617 bytes of rules whose classes appear in no source file and 4,872 bytes repeated verbatim within the same at-rule context; `npm run css:prune` reproduces both and reports zero once applied. Only after that did the ceiling move, to cover styling for surfaces that exist. The note beside it says to re-run the check before raising it again.

**Test data.** `npm run seed:challenges` walks sixteen challenges through the genuine lifecycle with the genuine actors — owner authors, `org:approver_technical` clears the technical gate, platform legal/finance/ops clear theirs, only `org:publisher` publishes — covering drafts at each authoring stage, partial approval, a rejected gate, public and registered visibility, verification and NDA gates, team-only applicants, an undecided budget, a near deadline, and paused and closed calls. `b2-approval-sessions.sql` supplies the local sessions that made this possible; it adds no role and widens no authority.

**The sync question, answered.** The owner reported that published challenges looked out of step between the two sides. All nine challenges published through the real route match their aggregate on every projected field; the two that drifted had been inserted by hand in an earlier session, and the projection's own immutability trigger now refuses to repair or delete them, which is the guard working. A new assertion compares every projected field against the version it was written from, which the previous literal-pinning assertions could not catch.

**Owner decisions still needed** (recorded in [95](95_RISKS_AND_OPEN_QUESTIONS.md) §4 with recommendations): what an organization may learn about a solver and when; how an organization addresses a direct offer to someone it has not worked with, given `recipient_workspace_id` is unusable by hand; and who may change a proposal's team assignment, which needs a canonical `teamActions` entry rather than an implementation choice.

**Evidence:** native 551/551; API 103/103; PostgreSQL 126/126; typecheck, lint, format, workspace boundaries; build, 519 routes, 520 link-checked files, smoke, offline, standalone-interactive; both demo and network budget sets. Verified in the browser at each phase rather than on the numbers alone, including the operator sign-out ending the session and `/app` resolving to that operator's own queue.

**Not done:** the proposal builder still writes its six-step draft through the browser repository; organization pilots, contracts, and reports remain later-phase surfaces; and the RTL/a11y/responsive rubric needs owner visual review, which cannot be self-certified.

### 2026-09-05 — C9 stage 1: gateway foundation and route truthfulness

**Scope:** C9 is roughly three times the size of C6-C8 combined, and it is the one slice where a partial conversion is worse than none: a half-rewired frontend leaves some pages live and some on fixtures, which is exactly the "looks live but isn't" failure C9 exists to remove. The owner chose to stage it. Stage 1 builds the foundation every later stage depends on and establishes the audit baseline; it deliberately converts no page family yet.

**Measured before starting:** 512 registered output routes, 33 files reading `localStorage`/`sessionStorage`/the demo repository, 2,006 lines of solver repository to replace, and two gateways in `RuntimeProvider` against the nine families C9 needs.

**What landed:** one shared `GatewayResult` so every family reports loading, validation, conflict, and revoked the same way and a page can render honest typed states; connected gateways for C1 profile, C2 team, C3-C5 proposals, C4 organization inbox and detail, and C8 notifications, each reading the active workspace at request time rather than at render time; those gateways wired into `RuntimeProvider` and left null in demo mode, so a demo page must show its preview boundary instead of receiving a gateway that quietly answers with fixtures; the `/app` workspace resolver, which the audit recorded as referenced but never registered, resolving one reachable workspace by entering it, several by an explicit chooser, none by a useful next action, and honoring `returnTo` only when it belongs to the persona that actually resolved; session-aware chrome extended from the desktop header to the mobile drawer and the footer through the existing demo-safe boundary; and a route classification module plus the behavior that locks it.

**The audit baseline, stated plainly:** 339 registered paths classify as 12 live, 212 preview, 45 unavailable, 70 redirect. Only the connected MVP surfaces are live today. That number is the measure of what stages 2 and beyond must convert, and the behavior fails if any connected MVP route is downgraded or a later-phase surface is marked live.

**Evidence:** native 478/478 across 65 files; typecheck, lint, format, workspace boundaries; static build emits `/app` at 513 unique routes with links and offline artifact verified; smoke passes; demo and network performance budgets pass.

**Explicitly not done in stage 1:** no page family has been converted, so the 33 files that read browser storage or the demo repository still do. The RTL/a11y/responsive rubric that C9's acceptance requires needs owner visual review; AGENTS.md forbids self-certifying visual snapshots, so that gate stays open regardless of how many stages land.

### 2026-09-05 — C9 stage 2: the solver proposal list and live dashboard counts

**Scope:** converting the first page family, the solver dashboard. Its blocking dependency was missing: `/api/v1/proposals` was POST-only, so neither the dashboard counts nor the proposal list page had a workspace-scoped list to read.

**Added:** `GET /api/v1/proposals` returns the active solver workspace's own proposals. It carries list facts only — id, challenge, state, tracking code, version, readiness, timestamps — and deliberately omits content and version history, so a list cannot become a way to bulk-read drafts. The in-memory adapter mirrors the same rule so demo and connected runtimes cannot diverge.

**Frontend:** `readSolverDashboardSummary` reads profile, proposals, and notifications concurrently for the active workspace. A family that fails contributes `null` rather than zero, because a dashboard showing "0 drafts" when the drafts request was denied is the fake-count failure C9 exists to remove; one failure degrades that card alone. The dashboard prefers these live counts and keeps its local projection only in demo mode, and the hook re-reads when the active workspace changes, so switching refreshes the numbers instead of leaving the previous workspace's on screen.

**Defect found by the live check:** the PostgreSQL list applied workspace scope but not the `edit-proposal` gate that `getScoped` applies, so a team viewer could enumerate proposals they are refused when opening one — states, tracking codes, and readiness. The in-memory adapter did apply the gate, so the two runtimes had already diverged. The gate is now evaluated per row, with team authority resolved twice rather than once per row since only the assignment term varies.

**A regression test that first proved nothing:** the initial version created the proposal in the individual workspace, so the viewer saw an empty list from workspace scoping alone and passed with the gate removed. It now creates the proposal in the team workspace and fails against the ungated code, which is what makes it evidence.

**Evidence:** PostgreSQL 124/124 across 12 files; native 484/484 across 66 files; API 103/103; contracts 15/15; live route review 11/11 covering empty list, draft appearance, content omission, server readiness, submitted state and tracking code, cross-workspace isolation, viewer refusal, organization refusal, and unauthenticated refusal; typecheck, lint, format, workspace boundaries; `docker:smoke` with restart persistence.

**Remaining in C9:** the other page families — proposals list and builder, teams, saved and offers, organization inbox and detail, notifications — plus the RTL/a11y/responsive rubric, which still needs owner visual review.

### 2026-09-05 — C9 stage 4: connected sign-in, live chrome, and solver/organization parity

**Scope:** making the connected application reachable from a browser at all, then removing the divergences between the solver and organization sides that only became visible once both were live.

**Sign-in existed only as an API.** No frontend code called C7, and the two commands that mint a session returned bearer tokens in their response body — correct for a service client, wrong for a browser that keeps credentials in HttpOnly cookies. Two same-origin routes now run those same commands and set the cookies instead, so a solver signs in and activates without a token reaching client JavaScript. The login and registration pages share one OTP flow with no password field and no team-account tab, because the server already knows whether a verified contact has an activation; asking a human to choose "sign in" or "register" first is a question it can answer for them.

**A returning solver had no active workspace.** Activation set the session's context; the returning-contact exchange left it null while already reading the individual workspace it needed. The result was a signed-in session no workspace-scoped read could use: `/app` offered a chooser for the single workspace it had, and every connected page family fell back to its anonymous state. Both adapters now enter the permanent individual workspace, as activation does, and `/app` resolves a sole reachable workspace rather than presenting a choice of one. The existing test asserted the null and was updated with the reason.

**Chrome showed a fixture person under a live session.** The solver shell read identity, the workspace list, and the unread badge from the demo repository, so a connected session landed on a dashboard captioned with someone else's name and a fixture team list. That is worse in chrome than in a page body: the sidebar is the one thing a human trusts to say whose workspace they are in. Identity, the switcher, and the badge are now server facts, and workspace switching is a command rather than a URL rewrite. Connected links no longer carry a `space`/`workspaceId` pair, which was a demo identifier in the address bar of a page reading a different, real workspace.

**Parity work.** Both shells now share one unread-count hook, so the two personas cannot disagree about when a badge clears. The solver dashboard resolves published challenge titles from B5's public projection, as the organization dashboard already did, instead of showing an opaque id for the same call. The organization login offered "enter the organization workspace" to any authenticated session, including a solver, who then met a refusal; it now checks that the session can actually reach one and otherwise offers the real path.

**Dashboard.** The team workspace was the personal dashboard with a different heading; it now carries its team, membership, and roles. Rows awaiting action sort above settled ones. Metric cards link to the list already filtered to the states they counted — and the list now reads that filter, groups by the same table the counts use, and always offers the four canonical groups, because filtering by exact state showed three on the card and one row on the list, and an empty workspace could not express the filter at all.

**Payload.** Connected sign-in, the connected dashboard cards, and the connected proposal record had been reaching the demo bundle, which had grown 146 KB over its ceiling. `sideEffects: ["*.css"]` lets webpack drop the network-only gateway graph the `RuntimeProvider` references but the static export never calls; the connected auth flow is loaded on demand; and the legacy solver dashboard and proposal list lost their connected branches entirely, since network mode renders `ConnectedSolverRoute` and those branches were unreachable. That recovered 137,206 bytes. The ceilings then rose by the measured remainder — 8,045 bytes on demo, 5,260 on network — with the reason recorded in `config/performance-budgets.json`.

**A regression in the shared shell.** The sidebar bottom had lost its solver guard, so the solver's identity appeared twice in one frame. Sign-out is a real need for a connected session, so the block now renders for solver with sign-out alone; two architecture tests that pinned the old source text were updated to assert what must not be there rather than the absence of the container.

**Evidence:** native 507/507 across 70 files; API 103/103; PostgreSQL 125/125; an 8/8 organization parity check against the running API proving the org side reads its own families and is refused every solver-scoped one; a 12/12 browser sign-in check covering start, verify, cookie session, `/me`, returning sign-in with Persian digits, and cross-origin refusal; and a real-browser walk of activation, sign-in, the dashboard, and the filtered proposal list. Typecheck, lint, format, boundaries, build, 519 routes, 520 link-checked files, smoke, offline, standalone, and both runtime budgets pass.

**Not verified here:** the organization OIDC login needs `dex.localhost`, which Compose supplies through `extra_hosts` and a host-run API cannot resolve, so that path was exercised on the Docker stack rather than the host one.

### 2026-09-05 — C9 stage 3 and the full Phase-3 live check

**Scope:** converting the page families stage 2 left on browser and demo authority — the solver proposal list and record, teams, saved opportunities and direct offers, the organization proposal inbox and record, and notifications on both sides — then walking the complete C1–C9 journey against the running Compose stack.

**Frontend:** every family now reads and writes the server for the active workspace in network mode and keeps its local projection only in the static export. None of them blends the two: a page that read live members and issued demo commands would show a receipt for a change the server never made, so each family dispatches wholly to one side or the other. One hook, `useConnectedFamily`, resolves the five states — demo, loading, anonymous, scope-lost, ready — for all of them, and `ConnectedFamilyFallback` renders the non-ready ones identically and without enumerating. A failed read contributes an explicit error, never a zero or an empty list.

Server-generated proposal ids reach the UI through pre-generated record paths carrying `?id=`, the convention the challenge slice already established, because `dynamicParams` takes a literal and the static export must pre-generate every route. An absent or malformed id renders an explicit unavailable state and never falls back to a known fixture.

**Contract gap closed:** an organization had no way to read the proposal version its own C4/C5 commands require. The record read was unversioned and `OrganizationProposalResource` carried no version, so the contract asked for a token it never handed out and only a caller that already knew the sequence — a test — could issue those commands. The record read now carries the aggregate version in both the body and `meta.entity_version`, exactly as the solver's own read does; the inbox row stays closed.

**Three defects found by the live check:**

Migration `0021` removes a second clock from two C6 triggers. Both compared a response deadline against `transaction_timestamp()` for an invariant the command path had already decided against the authoritative application clock, which returns a typed `422` or `409` naming the field. Two clocks for one invariant means a write is admissible under the clock that authorised it and refused by the clock that stores it whenever the two differ, and the caller receives an opaque database exception instead of the typed answer. Every invariant the triggers uniquely guard — sender ownership, exact published version, open call, deadline within the call's own deadline, offer state — is a comparison between stored values and is kept. This was a pre-existing failure on the branch, reproducible with the stage-3 changes stashed.

`mark-all-read` worked exactly once per workspace, ever. `mutation_receipt` is unique on `(entity_type, entity_id, entity_version)` and the command is addressed at the workspace with a constant version, so every later call collided and returned `503 STORAGE` with a retry hint that could never succeed. Receipts for these projection commands are now numbered per target, serialized by an advisory lock on the target so two concurrent commands cannot read the same maximum.

The proposal list blanked itself in demo mode: the early return tested a JSX element rather than the state kind, and an element is always truthy even when the component renders null. The existing component test caught it.

**Payload:** connected page components are loaded on demand, so the demo shared bundle fell by 4,761 bytes. The connected route ceilings rose by the measured 4,153 bytes the typed command gateways for nine families cost, with the reason recorded in `config/performance-budgets.json`.

**Evidence:** a full Phase-3 live check of **59/59** against the running stack, walking C7 activation with Persian digits and a masked destination, C1 profile and versioned eligibility, C2 team read/invite with a stale-version conflict, C6 save/unsave and the direct-offer lifecycle, C3 drafting, C4 submission and the organization inbox, C5 clarification requested/answered, C8 projection on both sides with unread counts and mark-read, API and worker restart persistence, and the C9 connected route surface — including that a foreign record and an absent one are now indistinguishable, that the inbox row and notification rows carry no aggregate content, and that the pre-rendered record page leaks none. PostgreSQL 125/125 across 12 files; native 496/496 across 67 files; API 103/103; typecheck, lint, format, workspace boundaries, source analysis; build, routes 518, links 519, smoke, offline, standalone-interactive; both demo and network budgets; `docker:smoke` with restart persistence.

**Remaining in C9:** the proposal builder still writes its six-step draft through the browser repository, and organization dashboard, profile, settings, access, and experts remain fixture surfaces classified `preview`. The RTL/a11y/responsive rubric needs owner visual review, which cannot be self-certified.

### 2026-09-05 — C7 review: activation verified live, one Persian-first defect fixed

**Scope:** reviewing C7 against the running stack — contact verification, activation, returning sign-in, and the abuse controls — plus the PostgreSQL suite and the static gates.

**Defect: the contract rejected Persian digits before the provider could normalize them.** `VerifyContactBody.code` was `^[0-9]{5}$`, ASCII only, while the development provider implements `normalizeDigits` for Persian and Arabic-Indic digits. Schema validation runs first, so a solver entering `۱۲۳۴۵` on a Persian keyboard received `422 VALIDATION` on `/code` and the normalization the team wrote was unreachable over HTTP — a Persian-first product refusing the digits its own keyboards produce. The pattern now accepts ASCII, Persian, and Arabic-Indic digits; the provider still normalizes before comparison. An API regression covers it.

**Verified as specified:** a mobile solver starts verification, verifies with the development code, activates, and receives a session; `start_intent: "team"` still yields exactly one permanent individual workspace, matching the migration's `workspace_one_individual_per_owner_idx`; a returning solver exchanges a contact session without re-activating; a consumed verification token cannot activate twice; a forged token is refused. The start response masks the destination (`0912••••567`) and never carries the code.

**Abuse controls behave as designed:** five failed codes lock the attempt with a typed `VERIFICATION_LOCKED`, and repeated starts return `429 RATE_LIMITED`. C7 adds both codes to the canonical envelope rather than overloading existing ones.

**Reviewed and accepted:** the development OTP provider compares its static configured code with a plain string comparison rather than a constant-time one. It refuses production startup and requires `SOLVER_CONTACT_VERIFICATION_PROVIDER=development`, so the compared value is a known development constant, and the assertion token — the part that actually authorizes — is verified with `timingSafeEqual`. Migration `0019` keeps OTP codes and provider payloads out of PostgreSQL, storing only the consumed assertion id, and makes activation and consumption evidence append-only.

**Evidence:** PostgreSQL 116/116 across 11 files; native 473/473 across 64 files; API 103/103; contracts 15/15; live route review 13/13; typecheck, lint, format, workspace boundaries; `docker:smoke` with restart persistence.

**Operational note:** an `.env` created before C7 needs a manual `SOLVER_OTP_FLOW_SECRET`, since the initializer deliberately never rewrites an existing local file. `85_DEVELOPMENT_GUIDE` documents this; without it `docker compose` refuses to interpolate and the stack will not start.

**Verdict:** pass at the native code/contract/API boundary; C3 remains `verification` until owner-run PostgreSQL/Docker execution and independent review.

### 2026-09-04 — C4 authoritative proposal submission and organization reads

**Scope:** exact-rule proposal submission, locked-version/grant migration hardening, typed Fastify/OpenAPI contracts, scoped organization inbox/detail projections, and domain/contract/API/PostgreSQL/worker tests. Reviewed against `20_CANONICAL_MODEL`, `50_DATA_MODEL`, `60_API_CONTRACT`, `70_SECURITY_AND_AUTHZ`, and the tenant/current-authority/atomic-evidence/immutable-version constraints in `AGENTS.md`.

**Positive controls verified (native boundary):** submit revalidates current solver/team membership, C2 `submit-proposal` policy and assignment before idempotent replay; the PostgreSQL adapter row-locks the proposal and immutable exact challenge/rule/C1 facts and uses `transaction_timestamp()` for deadline and evidence. Required verification is conditional on that exact rule, while IP/conflict/accuracy declarations remain unconditional and NDA follows the rule. One successful transaction appends a new exact-base locked version, advances the aggregate, assigns tracking/time, creates one version-bound read grant to the challenge owner, and records one receipt/audit/metadata-only outbox/idempotency result. Proposal grants reject delete/reactivation and preserve their exact binding through a C4 down/up cycle. Organization reads begin from an active/unexpired grant and active organization context, require challenge ownership, expose only the granted locked version, and collapse unknown/revoked/expired/foreign records to `NOT_FOUND`; the inbox SQL does not retrieve proposal content.

**Residual risks / untested boundaries:** native typecheck and focused domain/contract/API/worker tests pass. The focused PostgreSQL migration/adapter suite is authored but intentionally not executed because the owner reserved Docker verification for the pushed branch. Same-session review is not independent assurance. The access grant currently uses a 30-day operational expiry pending an explicit owner duration decision. C5 clarification/revision, C8 notifications, C9 connected UI, G3 private files, production RLS/rate limits, and deployment are not claimed.

**Verdict:** C4 is implemented at the native code/contract/API boundary and remains `verification` pending owner-run PostgreSQL/Docker execution and independent review.

### 2026-09-05 — C7 self-service solver activation

**Scope:** development contact-verification provider contract, email/mobile verification lifecycle, first solver activation, returning contact sign-in, permanent individual workspace invariant, optional separate C2 team continuation, migration `0019`, and domain/contract/API/PostgreSQL/worker tests. Reviewed against ADR-0010, DEC-2026-016, `50_DATA_MODEL`, `60_API_CONTRACT`, and the C7/C9 split in [84](84_PHASE3_CONNECTED_MVP_AUDIT.md).

**Implemented behavior:** anonymous start/resend/verify commands return masked contact metadata and typed expiry, lockout, rate-limit, stale-version, replay, and denial states. The local provider accepts normalized email or Iranian mobile input, signs short-lived assertions, bounds starts/sends/failures, emits `Retry-After`, and refuses production startup. First activation consumes one assertion and commits the provider-linked app user, solver tenant, exactly one owned individual workspace and membership, blank C1 profile, independent `not_started` verification, active session, immutable activation, receipt, audit, metadata-only outbox, and credential-scoped replay result together. A fresh assertion signs an existing solver in again without creating another identity or workspace.

**Team-intent boundary:** `start_intent=team` creates no team credential and no team inside the activation transaction. It returns `create_team`; the existing C2 command owns team/profile/owner-membership/verification creation. API coverage proves a failed C2 attempt leaves the personal activation usable, and a later successful retry can switch the same session into the created team workspace.

**Review refinements:** the Docker composition now uses the dedicated generated OTP-flow secret rather than reusing the OIDC secret. The older OIDC exchange path now fails closed instead of dereferencing a null email for C7 mobile-only users. PostgreSQL activation locks by provider identity rather than assertion token, so two valid assertions for one contact cannot race past the identity existence check. Database execution additionally caught and fixed a null-byte advisory-lock key that PostgreSQL rejects and untyped polymorphic JSON parameters that prevented every activation audit/outbox insert. Successful resend progression, resend ceiling, and `Retry-After` are pinned by tests.

**Evidence:** typecheck; domain 28/28, contracts 15/15, API 102/102, worker 10/10; full native repository 472/472; focused C7 plus migration tests 15/15; full PostgreSQL 116/116 across 11 files; API/worker/network production builds; Compose configuration/startup; Docker restart-persistence smoke; and a live containerized start → verify → activate → authenticated `/me` check proving one individual workspace. Format, lint, and workspace boundaries pass. The PostgreSQL run covers the full atomic graph/replay, mobile-only activation and post-restart sign-in, concurrent same-contact activation, append-only evidence, and compatibility of the complete migration ladder. C7 remains `verification` pending owner/independent review and the later C9/C10 connected-browser gates.

**Not claimed:** C9 connected forms/session-aware chrome, C10 browser certification, managed production contact delivery/identity, MFA/step-up, RLS, platform-wide abuse controls, private files, or operated worker delivery.
