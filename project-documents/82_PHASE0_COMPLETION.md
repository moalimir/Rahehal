# Phase 0 — Baseline & Decisions: completion report

Tracks the Phase-0 deliverables and exit gates from [80 §3](80_DELIVERY_ROADMAP.md). Legend:
**✅ Done / verified** · **🟡 In progress — needs a fix or a decision** · **⬚ Owner action** (a business/legal sign-off).

Installed and verified locally on 2026-08-23 (macOS arm64, Node 24.16.0 / npm 11.13.0 — within the pinned `engines` floor; CI runs the pinned Node 22 from `.nvmrc`).

## 1. Verified locally (2026-08-23)

| Check                                                              | Result                                                                                                                                      |
| ------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------- |
| `npm ci --include=optional`                                        | ✅ 644 packages installed / 750 dependency nodes audited, **0 vulnerabilities**, `@img/sharp-libvips-darwin-arm64` present — **R-10 fixed** |
| `npm run typecheck`                                                | ✅ pass                                                                                                                                     |
| `npm run lint`                                                     | ✅ pass (`--max-warnings=0`)                                                                                                                |
| `npm run format:check`                                             | ✅ pass                                                                                                                                     |
| `npm test`                                                         | ✅ **49 files, 354 tests** pass across web, API, worker, and shared packages                                                                |
| `npm run build`                                                    | ✅ **513 HTML files / 512 unique route outputs**, offline bundle regenerated, **no manual retry**                                           |
| `verify:routes` / `verify:links` / `test:smoke` / `verify:offline` | ✅ all pass                                                                                                                                 |
| `test:standalone-interactive`                                      | ✅ **now passes** — was a harness bug (T-C), fixed in `scripts/smoke-standalone-interactive.mjs`; the offline bundle renders correctly      |
| `test:browser` (Playwright, real Chromium)                         | ✅ **66 passed across 7 viewports; 4 intentional non-mobile skips; 0 failures**                                                             |
| `check:budgets` / `check:budgets:network`                          | ✅ runtime-specific shared, largest-asset, representative-route, CSS, and offline limits pass under DEC-2026-014                            |

## 2. Deliverable status

| #   | Phase-0 deliverable                                  | Status                          | Notes                                                                                |
| --- | ---------------------------------------------------- | ------------------------------- | ------------------------------------------------------------------------------------ |
| 1   | Resolve P0 decisions with named owners               | ✅ (defaults) / ⬚ (sign-off)    | [25_DECISIONS.md](25_DECISIONS.md) Part B                                            |
| 2   | Adopt and converge the canonical model               | ✅                              | ADR-0001; X-02/X-03/X-04 executable, including challenge-store v9 migration          |
| 3   | Open ADRs (ADR-0001…0014)                            | ✅                              | [25_DECISIONS.md](25_DECISIONS.md) Part A                                            |
| 4   | Pin Node/npm runtime                                 | ✅                              | `.nvmrc`, `.node-version`, `engines`, `packageManager`                               |
| 5   | Deterministic optional-dep (Sharp/libvips) install   | ✅ **verified**                 | libvips present, build clean                                                         |
| 6   | Environment contract                                 | ✅                              | `.env.example`                                                                       |
| 7   | `SECURITY.md`, `LICENSE`, `CONTRIBUTING.md`          | ✅                              | CODEOWNERS/PR-template omitted (lean agentic workflow)                               |
| 8   | CI running every release gate                        | ✅ defined / 🟡 first Linux run | `.github/workflows/ci.yml`                                                           |
| 9   | Contribution / handoff guide                         | ✅                              | `CONTRIBUTING.md`                                                                    |
| 10  | Offline bundle regenerates + standalone smoke passes | ✅ **verified**                 | smoke fixed (T-C); regenerated `index.html` committed and verified                   |
| 11  | Restore/rebaseline perf budgets                      | ✅                              | rebaselined per DEC-2026-009; payload reduction moves to the hardening gate          |
| 12  | Browser behavior + visual baseline                   | ✅ behavior / 🟡 visual         | behavior green in real Chromium; visual Golden Master needs human review (T-E)       |
| 13  | Reproducible green frontend                          | ✅ **verified**                 | see §1                                                                               |
| 14  | Local Docker web/API/worker integration              | ✅ **verified**                 | Compose health + restart-persistence smoke; connected A3 and B7 suites pass 3/3 each |

## 3. Remaining tasks

Debugged this session: **T-A, T-B, T-C, T-D, and the behavior half of T-E are done.** What's left:

| Task                              | Owner                   | Status / acceptance criterion                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| --------------------------------- | ----------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ~~T-A: reproducible install~~     | Platform                | ✅ **Done** (macOS arm64; confirm on CI Linux at first run)                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ~~T-B: green baseline~~           | Platform                | ✅ **Done**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ~~T-C: offline standalone smoke~~ | Frontend                | ✅ **Done** — harness race + jsdom-27 parser noise fixed in `scripts/smoke-standalone-interactive.mjs`; app renders correctly                                                                                                                                                                                                                                                                                                                                                                                   |
| ~~T-D: Performance budgets~~      | Frontend + Architecture | ✅ **Done** — DEC-2026-014 replaces the exhausted total-JavaScript ceiling with demo/network shared, largest-asset, and representative-route initial-load gates; emitted totals remain visible trends and p75 web-vital evidence remains a pilot gate                                                                                                                                                                                                                                                           |
| **T-E: Visual Golden Master**     | Frontend + Design       | Behavior ✅ done. Remaining: review 18 visual scenarios × 7 viewports and commit the Golden Master to `playwright/golden/` (needs human sign-off; `npm run test:visual:update`). Moved out of `ci.yml` into the manually-triggered `.github/workflows/visual.yml` — it cannot pass without a baseline, so running it on every push was pure CI cost (~8 min) for zero signal. Run it on demand via the Actions tab when doing the review; fold it back into `ci.yml` as a required job once the baseline lands. |
| **T-F: Owner sign-offs**          | per DEC                 | Complete [27_PHASE1_OWNER_APPROVALS](27_PHASE1_OWNER_APPROVALS.md), including DEC-2026-005/010/011 and the concrete IdP/region/provider evidence; other phase-specific DEC approvals remain tracked in 25                                                                                                                                                                                                                                                                                                       |
| **T-G: Replace placeholders**     | Owner                   | Real security contact in `SECURITY.md`; legal-confirmed `LICENSE`/brand                                                                                                                                                                                                                                                                                                                                                                                                                                         |

> `ci.yml` no longer has any `continue-on-error` job — every job that runs automatically is required. Visual regression only runs when manually dispatched.

## 4. Exit-gate status ([80 §3](80_DELIVERY_ROADMAP.md))

| Exit gate                                                            | Status                                                                 |
| -------------------------------------------------------------------- | ---------------------------------------------------------------------- |
| A clean clone passes every release command with no manual workaround | ✅ all required gates green locally; confirm the first run on CI Linux |
| No checked-in/generated artifact is stale                            | ✅ offline bundle regenerated, committed, and verified                 |
| Performance + browser + visual gates green or signed-with-expiry     | 🟡 performance + behavior ✅; visual Golden Master (T-E) remains       |
| MVP scope, data classes, roles, launch boundary have named owners    | ✅ decided / ⬚ pending T-F sign-offs                                   |

## 5. Verdict

The **decisions half is complete** and the **repository baseline is verified working on this machine**: install (R-10 fixed), typecheck/lint/format/tests, production build, static/offline gates, and real-browser behavior are green. The localhost-only Docker stack is healthy and now has a clean native `linux/arm64` multi-stage rebuild from the pinned base; Linux CI is still required as the independent `linux/amd64` confirmation.

**The remaining baseline work is human review and external confirmation:** the visual Golden Master (**T-E**), owner sign-offs / placeholder replacements (**T-F/T-G**), and the first Linux CI run. None blocks the implemented local delivery phases.

## 6. Phase-1/2 execution checkpoint (2026-08-31)

Phase 1 (A1a–A3) and the MVP-first Phase 2 (B1–B8) are implemented locally. The connected browser is server-authoritative for the scoped organization challenge list, draft create/read/save, rough-brief ops triage, full-readiness transitions, attributed publication decisions, rejected-version rework, publish, reasoned live-call management, and public catalogue/detail. Platform legal/finance/ops use a purpose/gate-scoped queue plus allowlisted briefs; the full org aggregate remains same-workspace only.

Clean native `linux/arm64` API/web/worker images were rebuilt from the pinned Node 22 base on Docker Desktop, `database-setup` applied migration `0012_phase2_review_closure`, every service became healthy, and `docker:smoke` proved PostgreSQL persistence across API restart. `test:browser:connected` and the expanded `test:browser:b7` each pass 3/3. B7 now sends the rough brief through a visible platform-ops screening action before four distinct actors clear the publication gates; it then publishes, pauses, resumes, extends the deadline, discovers the call publicly, and opens only its allowlisted projection. The frontend/backend synchronization branch is incorporated in this combined tree and uses the authoritative `/me`, organization list, and stable deep links without a demo fallback.

The final local gate passes 59 Vitest files / 415 tests, 6 PostgreSQL files / 79 tests, 66 applicable demo-browser checks (4 viewport-specific skips), both connected browser suites at 3/3, the configured demo/network performance budgets, and the route/link/offline/standalone checks. DEC-2026-014 now reports the complete emitted JavaScript totals as trends (demo reference 1,818,758 bytes; connected reference 1,864,987 bytes) while hard-gating the largest asset, shared initial payload, and seven representative initial route payloads per runtime. Connected-only session/routing, governance, and public-projection components remain excluded from the demo bundle.

Phase 3 has begun but has not completed its delivery gate. Migration `0013` creates the solver-owned proposal/version foundation. C1 is built and remains in `verification`: migration `0014`, typed profile/verification/gate contracts, server routes, demo/PostgreSQL adapters, and the exact-rule/live-state eligibility evaluator are present. Its focused PostgreSQL suite and connected-boundary browser suite were executed against the local Docker stack after owner review fixes; independent assurance remains. C2 is also built and in `verification`: migration `0015`, the shared exhaustive `decideTeamPermission` matrix, typed team contracts/OpenAPI, authoritative demo/PostgreSQL adapters, scoped routes, recipient/requester binding, reasoned atomic evidence, row-locked member/owner changes, and deferred exactly-one-owner constraints are present. The owner exercised C2 in Docker and returned the membership-delete and transaction-denial audit refinements now on the branch; independent assurance and C8/C9 remain.

C3 is built and in `verification`: typed `POST/GET/PATCH` proposal draft routes now compose private demo/PostgreSQL adapters over migration `0013`. Draft creation requires a registered-reachable projection plus current open/unexpired aggregate state, and team creation/editing consumes the C2 decision matrix with current membership and assignment. Every save appends an unlocked exact-base version with computed changed fields/content hash and atomically advances the aggregate with receipt/audit/outbox/idempotency evidence. Native typecheck and domain/contract/API/worker tests pass; the focused PostgreSQL suite covers scope, current-role, replay/stale, concurrent create/save, and rollback, and has now been executed against Docker (5/5, full suite 97/97) alongside a 29/29 live route check. That execution exposed one defect, fixed in `c44c7d9`: locking the proposal row and joining `proposal_version` in a single statement made a concurrent save's loser read zero rows under READ COMMITTED, returning `404 NOT_FOUND` instead of a `409 CONFLICT` with `refetch_and_retry`. Owner and independent review remain. Attachment IDs remain metadata only.

C4 is built and in `verification`: migration `0016` makes locked proposal evidence fully append-only and binds each proposal/read grant to the exact locked version, solver owner, and challenge-owning organization. The authoritative submit route rechecks current C2 submit authority before replay, evaluates the exact C1/rule/live-call facts under one server timestamp, returns actionable typed gate failures, and commits the new locked version, submitted aggregate/tracking, time-bounded grant, receipt, audit, metadata-only outbox, and idempotency result atomically. Organization inbox/detail routes start from an active/unexpired grant plus active membership/workspace and challenge ownership, expose only the exact submitted projection, and return non-enumerating `NOT_FOUND` outside scope. Native typecheck and focused domain/contract/API/worker tests pass. The PostgreSQL migration/adapter suite has now been executed against Docker (103/103 across 9 files) alongside a 22/22 live route check. That execution exposed a blocker fixed in this pass: migration `0016` declared a `proposal%ROWTYPE` variable inside a multiple-item `INTO` list, which PL/pgSQL rejects, so `0016` could not apply and every PostgreSQL suite failed. Three C3-era foundation behaviors also still locked a version by `UPDATE`, the path `0016` deliberately closed, and were ported to the append-only insert. Owner and independent review remain. The 30-day grant expiry is an operational default pending an explicit duration decision.

C5 is built and in `verification`: migration `0017` adds controlled clarification and revision-request records, separates aggregate concurrency from content-version numbering, validates resubmission against an active unexpired request, and extends exact-version grants to resubmitted evidence. Eight typed commands carry the proposal from `submitted` through eligibility review, clarification, review, revision draft, and `resubmitted`; clarification text remains separate from proposal versions, while revision drafts and resubmission retain exact base links and compute the final diff against the requested locked version. Resubmission revokes the superseded grant and creates the replacement grant in the same transaction as the locked version, request progression, receipt, audit, metadata-only outbox, and idempotency result. Native typecheck and domain/contracts/API/worker tests pass, and the PostgreSQL suite runs against Docker: 106/106 across 9 files after this pass added the missing rejection behavior. Live route review of the whole eight-command lifecycle passed 27/28. Two defects were fixed. An `ineligible` decision passed an empty `next_actions`, which `mutation_receipt_next_actions_check` rejects, so every rejection failed with a database error and left the proposal stuck in `eligibility_review`; the terminal decision now reports `closed`, matching the challenge lifecycle's own terminal marker. Separately `npm run db:reset` walked the down migrations, so `0017`'s evidence guard — correct in itself — made the documented local reset impossible on any database that had exercised C5; reset now recreates the schema and the guard still governs every real `db:migrate:down`. Owner/independent review, C8 notifications, C9 connected proposal UI, and C10 browser certification remain. C4–C10 follow amended DEC-2026-004 and DEC-2026-016 for development-provider activation, optional separate team onboarding, conditional verification at submission, bilateral notifications, frontend synchronization, and real-browser certification.

C6 is built and in `verification`: migration `0018` adds durable exact-version saved opportunities plus authoritative `direct_offer` and versioned `offer_response` records. Typed solver and organization routes cover bookmark save/list/unsave and offer send/list/read/view, response start/save/submit, decline, cancel, negotiation start, and lazy server-time expiry. Send atomically creates the offer and its exact collaborate-offer/read-challenge grants; decline/cancel/expiry close both. The active response deadline applies through `response_draft`, and the sender cannot read draft response content before submission. Team commands use the same C2 permission oracle. Aggregate/response changes, grants, receipt, audit, metadata-only outbox, and idempotency remain transactional, while migration triggers protect identity, state progression, terminal/submitted evidence, and grant coherence. Native domain/contracts/API/worker tests and the full PostgreSQL suite pass (112/112 across 10 files, plus 464/464 native), and a live route review of the save/offer/response/negotiation flow passed 21/21. Completing and reviewing the slice fixed three defects: an offer to a workspace already holding a standing challenge grant collided on `0001`'s one-active-grant index and failed with `503 STORAGE`, so `0018` now scopes that rule to standing grants; the C6 grant trigger fell through to `RETURN NEW` on a DELETE and silently cancelled removal of any unrelated `access_grant`, repeating the C2 defect fixed in `2188d11`; and the eligibility evaluator judged offer grants by `transaction_timestamp()` while C6 writes their validity from the injected clock. C6's organization routes enforce `organizationCapabilities(role).manageDirectOffers`, which matches the canonical `organizationOfferManagers` exactly. Owner/independent review, C8 notification projection, C9 connected UI, G3 file authority, and Phase-4 selection/case creation remain.

C7 is built and in `verification`: migration `0019` permits email-only or mobile-only humans and records immutable solver activation plus one-time provider-assertion consumption without storing OTP codes or assertion tokens. Five typed contact/activation operations, a development-only provider, and demo/PostgreSQL adapters cover start, resend, verify, expiry, lockout, bounded requests, returning sign-in, exact idempotent replay, and one atomic identity/solver-tenant/permanent-individual-workspace/profile/verification/session activation. Team is only start intent and continues through the separate C2 command, so failure leaves the individual context usable. Native contracts/API/worker tests pass, and the full PostgreSQL suite passes 116/116 across 11 files, covering atomicity, restart sign-in, same-contact concurrency, append-only evidence, and migration compatibility. A live route review of start, verify, activation, returning sign-in, lockout, and rate limiting passed 13/13 and fixed one Persian-first defect: `VerifyContactBody.code` was ASCII-only, so schema validation rejected the Persian and Arabic-Indic digits the provider already normalizes and a solver typing `۱۲۳۴۵` received `422 VALIDATION`. The start response masks the destination and never carries the code, five failed codes lock the attempt with a typed `VERIFICATION_LOCKED`, and repeated starts return `429 RATE_LIMITED`. C9 connected auth/onboarding UI, C10 browser certification, the owner-supplied production provider, and independent assurance remain.

C8 is built and in `verification`: migration `0020` adds the `notification` read model and `outbox_delivery` evidence, and the worker moves from the in-memory skeleton to durable PostgreSQL claims. `PostgresOutboxSource` claims with `FOR UPDATE SKIP LOCKED` under a lease, retries, and dead-letters without deleting the row; `PostgresDeliveryLedger` commits the delivery record and the projection together, so at-least-once delivery produces exactly one notification per recipient. `notificationProjections` is the exact event-to-audience table, and an event absent from it produces nothing. The projector resolves recipients by reading the aggregate inside the delivery transaction rather than trusting the event payload, and stores only kind, subject reference, and timestamps -- no proposal or response content, clarification or revision text, contact details, or credentials. Bounded pages use a stable `(occurred_at, id)` cursor; unread counts, mark-read, and mark-all-read are scoped by recipient before id, so a foreign notification is non-enumerating. Projected facts are immutable and a read notification cannot be unread. Building it exposed two defects, both fixed: outbox claims compared `available_at` against the worker's wall clock instead of the database's, so clock skew silently delayed delivery; and the notification command routes did not escape the literal action colon, so every mark-read returned `422 VALIDATION`. C9's full Phase-3 live check later found a third: `mutation_receipt` is unique on `(entity_type, entity_id, entity_version)` and mark-all-read is addressed at the workspace with a constant version, so it succeeded exactly once per workspace and thereafter returned `503 STORAGE` with a retry hint that could never succeed. Receipts for these projection commands are now numbered per target, serialized by an advisory lock on the target so two concurrent commands cannot read the same maximum. Native, API, contract, worker, and PostgreSQL suites pass, and a live review of the submit-to-notification path passed 14/14 against the PostgreSQL worker. External email, SMS, and push delivery remain later work; G4 still owns poison-isolation, WORM export, and correlation search; owner and independent review remain.

C9 is in progress across three landed stages. Stage 1 built the connected foundation: a shared typed `GatewayResult`, workspace gateways wired into `RuntimeProvider` (null in demo mode, so a page shows its preview boundary rather than a gateway that silently returns fixtures), the `/app` workspace resolver the audit recorded as referenced but unregistered, session-aware public chrome, and a route classification locked by a behavior. Stage 2 converted the solver dashboard and added the workspace-scoped proposal list it needed; the live check found that the PostgreSQL list applied workspace scope but not the `edit-proposal` gate the record read applies, so a team viewer could enumerate what they are refused when opening. Stage 3 converted the remaining families -- solver proposal list and record, teams, saved opportunities and direct offers, the organization proposal inbox and record, and notifications on both sides -- behind one connected state machine that resolves demo, loading, anonymous, scope-lost, and ready identically everywhere; a failed read contributes an explicit error rather than a zero or an empty list. Server-generated ids are addressed through pre-generated record paths carrying `?id=`, and an absent or malformed id renders an explicit unavailable state rather than falling back to a fixture.

Running the whole Phase-3 journey live closed one contract gap and three defects. A granted organization had no way to read the proposal version its own C4/C5 commands require, so the contract asked for a token it never handed out; the record read now carries it in the body and in `meta.entity_version`, while the inbox row stays closed. Migration `0021` removes a second clock from two C6 triggers that judged a response deadline against `transaction_timestamp()` for an invariant the command path had already decided with the authoritative clock. `mark-all-read` worked exactly once per workspace. And the proposal list blanked itself in demo mode because an early return tested a JSX element, which is always truthy, rather than the state kind. The full Phase-3 live check passes 59/59 across C1-C9 including API and worker restart persistence, with PostgreSQL 125/125, native 496/496, and every static gate and both runtime budgets. Remaining in C9: the proposal builder still writes its six-step draft through the browser repository; organization dashboard, profile, settings, access, and experts remain fixture surfaces classified `preview`; and the [83](83_UI_UX_AUDIT_PLAN.md) RTL/a11y/responsive rubric needs owner visual review, which cannot be self-certified. C10 browser certification remains.

This does not claim production readiness. Managed production OIDC/contact-provider approval, MFA/step-up, RLS, private files, platform-wide abuse controls, authoritative worker delivery, backups/restore, observability, and server deployment remain later roadmap gates. C7 now supplies only the development-provider flow and its local replay/expiry/resend/rate-limit protections. Phase 3 may continue from the completed local Phase-1/2 boundary; post-publish material amendment and publication override stay separate future exception paths.
